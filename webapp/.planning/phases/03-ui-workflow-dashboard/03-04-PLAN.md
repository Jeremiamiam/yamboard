---
phase: 03-ui-workflow-dashboard
plan: "04"
type: execute
wave: 3
depends_on:
  - "03-02"
  - "03-03"
files_modified: []
autonomous: false
requirements:
  - WF-01
  - WF-02
  - WF-03
  - WF-04
  - WF-05
  - WF-06

must_haves:
  truths:
    - "User can launch /start from project page and have a conversation using the start system prompt"
    - "User can launch /site-standalone independently and have a conversation"
    - "Locked workflows show no launch button"
    - "Marking /start complete unlocks /platform on the project page"
    - "chat?workflow=start URL shows 'Workflow start' subtitle and the completion button"
  artifacts:
    - path: "src/components/workflow-launcher.tsx"
      provides: "Interactive workflow list — visually confirmed"
    - path: "src/actions/workflows.ts"
      provides: "Workflow state updates — functionally confirmed"
  key_links:
    - from: "project page WorkflowLauncher"
      to: "chat page with workflowSlug"
      via: "Lancer button navigation"
      pattern: "?workflow="
---

<objective>
Human verification of the complete Phase 3 workflow UI. Confirm the full user journey: launch a workflow → chat with correct system prompt → mark complete → next workflow unlocks.

Purpose: Phase 3 delivers interactive UI. Automated TypeScript checks verify compilation but cannot verify the user experience: does the Lancer button navigate correctly? Does the agent respond with workflow-appropriate behavior? Does the completion button update the project page?

Output: Confirmed Phase 3 complete — all 6 WF requirements verified in browser.
</objective>

<execution_context>
@/Users/jeremyhervo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeremyhervo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and smoke-test</name>
  <files></files>
  <action>
Run the production build to catch any compilation errors:

```bash
cd "/Users/jeremyhervo/Agence Yam Dropbox/Yambox/_IA/getBrandon/webapp" && npm run build 2>&1 | tail -30
```

If build fails, diagnose and fix TypeScript/Next.js errors before proceeding.

If build succeeds, start the dev server for human testing:
```bash
cd "/Users/jeremyhervo/Agence Yam Dropbox/Yambox/_IA/getBrandon/webapp" && npm run dev
```
  </action>
  <verify>
    <automated>cd "/Users/jeremyhervo/Agence Yam Dropbox/Yambox/_IA/getBrandon/webapp" && npm run build 2>&1 | grep -E "(error|Error|✓|Failed)" | head -20</automated>
    <manual>Build output shows no errors. Dev server starts at localhost:3000.</manual>
  </verify>
  <done>npm run build completes without errors. Dev server starts at localhost:3000.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Phase 3 workflow UI in browser</name>
  <files></files>
  <action>
Human verification of the complete Phase 3 workflow UI in the running dev server.

What was built:
- WorkflowLauncher on project page with "Lancer" buttons for available workflows
- /projects/[id]/chat?workflow=[slug] routes each workflow to its dedicated system prompt
- "Marquer comme terminé" button in chat unlocks the next workflow
- Locked workflows display locked badge with no launch button
- /site-standalone available independently from the start of a project

Verification steps (5 tests):

**Test 1 — Workflow launcher UI (WF-06)**
1. Log in and open any project
2. Confirm project page shows "Lancer" buttons next to /start and /site-standalone
3. Confirm /platform, /campaign, /wireframe show "🔒 Bloqué" with NO Lancer button

**Test 2 — Launch /start (WF-01)**
4. Click "Lancer" on /start
5. Confirm URL is /projects/[id]/chat?workflow=start
6. Confirm chat header shows "Workflow start" subtitle
7. Send a message — confirm agent responds as Brandon conducting brand strategy workflow

**Test 3 — Launch /site-standalone (WF-04)**
8. Navigate back to project page
9. Click "Lancer" on /site-standalone
10. Confirm URL has ?workflow=site-standalone
11. Confirm agent responds in site director persona

**Test 4 — Mark complete + unlock (WF-02)**
12. Return to the /start chat
13. Send any message to start the conversation
14. Confirm "✓ Marquer comme terminé" button appears below the input
15. Confirm button is enabled when not streaming
16. Click "✓ Marquer comme terminé"
17. Navigate back to the project page
18. Confirm /start shows "✓ Complété" badge
19. Confirm /platform now shows "Disponible" badge with a "Lancer" button

**Test 5 — Locked workflows remain locked (WF-06)**
20. Confirm /campaign and /wireframe still show "🔒 Bloqué" with no launch button
  </action>
  <verify>
    <automated>cd "/Users/jeremyhervo/Agence Yam Dropbox/Yambox/_IA/getBrandon/webapp" && npx tsc --noEmit 2>&1 | head -5</automated>
    <manual>All 5 browser tests pass. No JavaScript errors in browser console. Workflow states update correctly in Supabase.</manual>
  </verify>
  <done>Human confirms all 5 tests pass. All WF-01 through WF-06 requirements verified in browser.</done>
  <resume-signal>Type "approved" if all tests pass, or describe which test failed and what you saw</resume-signal>
</task>

</tasks>

<verification>
All 6 WF requirements verified:
- WF-01: /start launched and produces workflow-specific agent response
- WF-02: /start completion unlocks /platform
- WF-03: /platform completion would unlock /campaign (pattern verified, same mechanism)
- WF-04: /site-standalone available and launches independently
- WF-05: /site-standalone completion would unlock /wireframe (pattern verified)
- WF-06: Locked workflows show no launch button; completed workflows show completed badge
</verification>

<success_criteria>
- Build succeeds with 0 errors
- WorkflowLauncher renders on project page with correct button states
- Each workflow launch URL reaches the chat with the correct system prompt
- "Marquer comme terminé" button updates workflow_states in Supabase and revalidates the project page
- Human verification approved
</success_criteria>

<output>
After completion, create `.planning/phases/03-ui-workflow-dashboard/03-04-SUMMARY.md`

Update STATE.md:
- Phase: 4 of 5 — READY TO PLAN
- Status: Phase 3 COMPLETE (human-verified)
- Progress: 80%

Update ROADMAP.md:
- Mark Phase 3 as [x] complete
- Update Phase 3 plans list to checked

Update REQUIREMENTS.md:
- Mark WF-01 through WF-06 as [x] complete
</output>
