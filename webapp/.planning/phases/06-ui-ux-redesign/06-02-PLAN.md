---
phase: 06-ui-ux-redesign
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/app/dashboard/page.tsx
  - src/app/projects/[id]/page.tsx
  - src/components/workflow-launcher.tsx
  - src/components/create-project-modal.tsx
  - src/components/delete-project-button.tsx
autonomous: true
requirements:
  - DSGN-02

must_haves:
  truths:
    - "Dashboard header has a clear visual identity with GET BRANDON wordmark"
    - "Project list uses card layout with hover states showing clear affordance"
    - "Workflow progress is visually shown (e.g., progress bar or count indicator)"
    - "Project page workflow section has clear information hierarchy — completed/available/locked visually distinct"
    - "Navigation (back links, wiki link) uses consistent styling"
  artifacts:
    - path: "src/app/dashboard/page.tsx"
      provides: "Redesigned dashboard with improved project list"
      min_lines: 60
    - path: "src/app/projects/[id]/page.tsx"
      provides: "Redesigned project page with visual workflow section"
      min_lines: 80
    - path: "src/components/workflow-launcher.tsx"
      provides: "Redesigned workflow cards with clearer status hierarchy"
      min_lines: 60
  key_links:
    - from: "src/app/dashboard/page.tsx"
      to: "src/components/create-project-modal.tsx"
      via: "CreateProjectModal component import"
      pattern: "CreateProjectModal"
    - from: "src/app/projects/[id]/page.tsx"
      to: "src/components/workflow-launcher.tsx"
      via: "WorkflowLauncher component import"
      pattern: "WorkflowLauncher"
---

<objective>
Redesign the dashboard and project page for improved information hierarchy and visual clarity.

Purpose: DSGN-02 — workflow cards, project list, and navigation need visual improvement so users can understand project state at a glance.
Output: Polished dashboard page, project page, workflow launcher, create modal, delete button.
</objective>

<execution_context>
@/Users/jeremyhervo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeremyhervo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/app/dashboard/page.tsx
@src/app/projects/[id]/page.tsx
@src/components/workflow-launcher.tsx
@src/components/create-project-modal.tsx
@src/components/delete-project-button.tsx
@.planning/phases/06-ui-ux-redesign/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign dashboard page — header, project list, empty state</name>
  <files>src/app/dashboard/page.tsx</files>
  <action>
Redesign the dashboard page with improved visual hierarchy. Keep all logic identical — only styling changes.

**Header improvements:**
- Keep the `bg-white border-b` header structure
- Add a subtle bottom shadow: `shadow-sm` on the header
- Style the "GET BRANDON" wordmark: `text-xl font-bold tracking-tight text-gray-900` (slightly larger than current)
- "Deconnexion" button: keep current style but use `text-gray-400 hover:text-gray-700`

**Main content:**
- Keep `max-w-3xl mx-auto px-6 py-8` wrapper
- Section header: "Projets" as `text-2xl font-semibold text-gray-900` (increase from xl)
- "Nouveau projet" button (CreateProjectModal): currently styled in the modal component — add a hint class `data-variant="primary"` to communicate intent (actual styling is in the component)

**Project list — from `ul divide-y` to card grid:**
Replace the simple list with a more visual card layout:
```tsx
<ul className="space-y-2">
  {projectsWithCount.map((project) => (
    <li key={project.id}>
      <div className="bg-white rounded-lg border border-gray-200 px-4 py-3 hover:border-gray-300 hover:shadow-sm transition-all flex items-center justify-between">
        <Link href={`/projects/${project.id}`} className="flex-1 min-w-0">
          <span className="font-medium text-gray-900 truncate block">{project.name}</span>
          <span className="text-sm text-gray-400 mt-0.5 block">
            {formatDate(project.updated_at)} · {project.completedCount}/6 workflows
          </span>
        </Link>
        {/* Progress bar */}
        <div className="mx-4 hidden sm:flex flex-col items-end gap-1">
          <div className="w-24 bg-gray-100 rounded-full h-1.5">
            <div
              className="bg-blue-500 h-1.5 rounded-full transition-all"
              style={{ width: `${(project.completedCount / 6) * 100}%` }}
            />
          </div>
          <span className="text-xs text-gray-400">{project.completedCount}/6</span>
        </div>
        <DeleteProjectButton projectId={project.id} />
      </div>
    </li>
  ))}
</ul>
```

**Empty state:**
Replace current minimal empty state with a more polished version:
```tsx
<div className="text-center py-20">
  <div className="text-4xl mb-4">📁</div>
  <p className="text-gray-500 text-sm mb-6">Aucun projet pour l'instant.</p>
  <CreateProjectModal />
</div>
```
  </action>
  <verify>
    <automated>cd "/Users/jeremyhervo/Agence Yam Dropbox/Yambox/_IA/getBrandon/webapp" && npm run build 2>&1 | tail -10</automated>
    <manual>Dashboard shows project cards with progress bars, empty state shows illustration</manual>
  </verify>
  <done>Dashboard redesigned: card list with progress bars, polished empty state, improved header. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Redesign project page + workflow-launcher + create-modal + delete-button</name>
  <files>src/app/projects/[id]/page.tsx, src/components/workflow-launcher.tsx, src/components/create-project-modal.tsx, src/components/delete-project-button.tsx</files>
  <action>
Redesign the project page, workflow launcher, create modal, and delete button.

**src/app/projects/[id]/page.tsx — header and section layout:**
- Header: add `shadow-sm` to the header; back link `← Projets` stays as-is; project name in `text-xl font-bold text-gray-900`; completed count badge becomes a rounded pill `<span className="text-xs text-gray-400 bg-gray-100 px-2 py-0.5 rounded-full">{completedCount}/6</span>`
- Wiki link: change from `text-blue-600 hover:text-blue-800` to a button-style link: `<Link href={...} className="ml-auto text-sm text-gray-600 border border-gray-200 px-3 py-1.5 rounded-lg hover:bg-gray-50 transition-colors font-medium">Wiki →</Link>`
- Section headers: keep `text-sm font-medium text-gray-500 uppercase tracking-wide mb-3` pattern
- Section wrappers: keep `bg-white rounded-lg shadow-sm` but add `border border-gray-100` for subtle definition

**src/components/workflow-launcher.tsx — workflow rows:**
Redesign each workflow row to be more visually informative:
- Make locked workflows visually dimmed (`opacity-60`) rather than just gray text
- Make completed workflows show a checkmark circle icon (plain text `✓` in a green circle span) before the label
- Available workflow row: give it a subtle left border accent `border-l-2 border-blue-500 pl-3` to indicate it's actionable
- StatusBadge for available: change to show `●` dot in blue before "Disponible" for a more modern look
- StatusBadge for completed: keep green pill style (already good)
- "Lancer" button: keep `bg-blue-600 text-white rounded-lg` but increase padding to `px-4 py-1.5` for better tap target
- "Reprendre" button: keep border style
- Overall list: replace `divide-y divide-gray-100` with `space-y-1` and add `py-2` padding to each `<li>` — removes hard dividers for a cleaner look

**src/components/create-project-modal.tsx:**
- "Nouveau projet" trigger button: change to a more polished style: `px-4 py-2 bg-gray-900 text-white text-sm font-medium rounded-lg hover:bg-gray-800 transition-colors` (rounded-lg instead of rounded)
- Modal backdrop: keep `bg-black/40`
- Modal card: change `rounded-lg` to `rounded-xl` for a more modern feel; add `p-7` instead of `p-6`
- Input: change `rounded` to `rounded-lg` for consistency
- Cancel button: add explicit `rounded-lg`
- Submit button: add `rounded-lg`

**src/components/delete-project-button.tsx:**
Read this file first to see current implementation, then style the delete button as an icon-only button with `text-gray-300 hover:text-red-500 transition-colors` and an `×` character or trash icon text. Keep the `confirm()` dialog logic intact.
  </action>
  <verify>
    <automated>cd "/Users/jeremyhervo/Agence Yam Dropbox/Yambox/_IA/getBrandon/webapp" && npm run build 2>&1 | tail -10</automated>
    <manual>Project page shows polished workflow rows: available has blue accent, completed has green badge, locked is dimmed</manual>
  </verify>
  <done>Project page + workflow launcher + modals redesigned. Build passes with 0 errors.</done>
</task>

</tasks>

<verification>
- `npm run build` completes with 0 TypeScript errors and 0 build errors
- Dashboard has project cards with inline progress bars
- Project page workflow section: locked rows dimmed, available row has blue left border, completed rows show green badge
- Create project modal uses rounded-xl corners
- Delete button is a clean icon-only button
</verification>

<success_criteria>
Dashboard and project page visually improved:
- Information hierarchy clear: project name > metadata > progress
- Workflow status hierarchy visually distinct (available = highlighted, completed = green, locked = dimmed)
- Navigation consistent (back links, wiki link as outlined button)
- Build passes: 0 errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-ui-ux-redesign/06-02-SUMMARY.md` with:
- What was implemented (which components redesigned)
- Key visual decisions (spacing, badge styles, progress bar approach)
- Files modified
- Build status
</output>
