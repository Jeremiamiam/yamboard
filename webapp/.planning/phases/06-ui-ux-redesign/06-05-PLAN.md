---
phase: 06-ui-ux-redesign
plan: 05
type: execute
wave: 4
depends_on:
  - 06-04
files_modified: []
autonomous: false
requirements:
  - DSGN-01
  - DSGN-02
  - DSGN-03
  - DSGN-04

must_haves:
  truths:
    - "Production build passes with 0 TypeScript errors and 0 build errors"
    - "All 4 DSGN requirements confirmed working in browser"
    - "Design tokens visible in globals.css"
    - "Dashboard shows improved project cards with progress bars"
    - "Chat interface has back navigation and improved message bubbles"
    - "UX states (loading, error, empty) are present on all interactive components"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Design tokens"
      contains: "--color-brand"
    - path: "src/app/dashboard/page.tsx"
      provides: "Redesigned dashboard"
    - path: "src/components/chat-interface.tsx"
      provides: "Redesigned chat"
    - path: "src/components/file-uploader.tsx"
      provides: "File upload with UX states"
  key_links:
    - from: "src/app/globals.css"
      to: "All pages and components"
      via: "Tailwind CSS custom properties"
      pattern: "--color-brand"
---

<objective>
Build gate (TypeScript + production build) then human verification of the complete Phase 6 UI/UX redesign.

Purpose: Catch any TypeScript errors introduced during the redesign before manual testing. Verify all 4 DSGN requirements are met visually in the browser.
Output: Phase 6 confirmed complete — green light for v1.1 milestone.
</objective>

<execution_context>
@/Users/jeremyhervo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeremyhervo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Production build gate</name>
  <files></files>
  <action>
Run the production build to verify 0 TypeScript errors and 0 build errors across all modified files.

```bash
cd "/Users/jeremyhervo/Agence Yam Dropbox/Yambox/_IA/getBrandon/webapp" && npm run build
```

If errors are found:
- Fix TypeScript errors in the relevant component files
- Re-run `npm run build` until clean
- Document any fixes in the summary

Only proceed to the human verification checkpoint once the build is clean.
  </action>
  <verify>
    <automated>cd "/Users/jeremyhervo/Agence Yam Dropbox/Yambox/_IA/getBrandon/webapp" && npm run build 2>&1 | grep -E "error|Error|✓|Route" | tail -20</automated>
    <manual>Build output shows "✓ Compiled successfully" or equivalent success indicator with 0 errors</manual>
  </verify>
  <done>npm run build exits with code 0. No TypeScript errors. No build errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — Phase 6 UI/UX complete</name>
  <files></files>
  <action>Human verification of Phase 6 UI/UX redesign — see how-to-verify for steps.</action>
  <verify>Human approval: type "approved" after testing all 10 browser checks below.</verify>
  <done>All 4 DSGN requirements confirmed working in browser. Human types "approved".</done>
  <what-built>
Complete Phase 6 UI/UX Redesign across 5 plans:
- Plan 01: Design tokens in globals.css (palette, border-radius, typography)
- Plan 02: Dashboard (project cards + progress bars) + project page (workflow hierarchy) + create modal
- Plan 03: Chat interface (header with back nav, message bubbles, footer polish)
- Plan 04: UX states (loading spinners, error alerts, empty states) + login page + wiki sections
  </what-built>
  <how-to-verify>
Run `npm run dev` in the webapp directory, then test each screen:

**DSGN-01 — Design System:**
1. Open browser DevTools → Elements → select `<html>` → check computed styles for `--color-brand`, `--color-bg`, `--radius-lg` custom properties in the `:root`
2. Confirm the tab title shows "GET BRANDON"

**DSGN-02 — Dashboard + Project Page:**
3. Visit `/dashboard`:
   - Project cards have hover states (border darkens + subtle shadow)
   - Projects show a progress bar (blue fill proportional to completed count)
   - Empty state shows centered icon + "Aucun projet" + "Nouveau projet" button
4. Click a project → `/projects/[id]`:
   - Available workflow row has blue left border accent
   - Completed workflows show green badge
   - Locked workflows are visually dimmed (opacity-60)
   - Wiki link in header looks like an outlined button

**DSGN-03 — Chat Interface:**
5. Open a workflow chat:
   - Header shows "← Retour" link, project name, workflow name subtitle
   - Send a message: user bubble is blue (right), assistant bubble is white with border (left)
   - "Marquer comme terminé" button appears ABOVE the input field (not below)
   - Input field has gray background that turns white on focus

**DSGN-04 — UX States:**
6. Upload a file: uploading shows spinner animation + "Envoi en cours..."
7. Delete a file: the specific file's delete button shows a spinner while deleting
8. Visit `/login`: page has "Outil de production agence" subtitle, form has rounded card
9. Login with wrong credentials: error shows with ⚠ icon in red alert box
10. Visit `/projects/[id]/wiki` (if outputs exist): TL;DR section has blue left-border highlight

Type "approved" if all checks pass, or describe what to fix.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
Human approval received on all 4 DSGN requirements:
- DSGN-01: Design tokens confirmed in DevTools, tab title correct
- DSGN-02: Dashboard cards, workflow status hierarchy visually distinct
- DSGN-03: Chat header navigation, message bubbles, footer layout
- DSGN-04: Loading spinners, error alerts, empty states on interactive components
</verification>

<success_criteria>
Phase 6 complete when:
- Production build passes: 0 TypeScript errors, 0 build errors
- Human verification: all 10 browser checks pass
- All 4 DSGN requirements confirmed (DSGN-01, DSGN-02, DSGN-03, DSGN-04)
- v1.1 milestone: Phases 4 + 5 + 6 all complete
</success_criteria>

<output>
After completion, create `.planning/phases/06-ui-ux-redesign/06-05-SUMMARY.md` with:
- Build status (0 errors confirmed)
- Human verification result (approved/issues found)
- DSGN requirements status: all 4 confirmed
- v1.1 milestone completion note
Update STATE.md: Phase 6 complete, v1.1 milestone shipped.
Update REQUIREMENTS.md: DSGN-01 through DSGN-04 marked [x].
Update ROADMAP.md: Phase 6 status updated to Complete.
</output>
