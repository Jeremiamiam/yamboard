---
phase: 06-ui-ux-redesign
plan: 04
type: execute
wave: 3
depends_on:
  - 06-02
  - 06-03
files_modified:
  - src/components/file-uploader.tsx
  - src/components/file-list.tsx
  - src/components/workflow-outputs.tsx
  - src/components/project-files-section.tsx
  - src/components/wiki-section.tsx
  - src/app/login/page.tsx
  - src/components/login-form.tsx
autonomous: true
requirements:
  - DSGN-04

must_haves:
  truths:
    - "File upload area shows uploading state with spinner animation"
    - "File list items show delete loading state per file (not just disabled)"
    - "Workflow outputs empty state has a helpful message (not just plain text)"
    - "Login page has a polished centered form with clear error state"
    - "Wiki sections have a polished layout with section headings and TL;DR highlighted"
    - "All interactive elements have visible focus rings for keyboard accessibility"
  artifacts:
    - path: "src/components/file-uploader.tsx"
      provides: "File upload with animated uploading state"
      min_lines: 70
    - path: "src/components/file-list.tsx"
      provides: "File list with per-file delete loading state"
      min_lines: 50
    - path: "src/app/login/page.tsx"
      provides: "Polished login page layout"
      min_lines: 15
    - path: "src/components/login-form.tsx"
      provides: "Login form with improved error state display"
      min_lines: 60
  key_links:
    - from: "src/components/project-files-section.tsx"
      to: "src/components/file-uploader.tsx"
      via: "FileUploader component import"
      pattern: "FileUploader"
    - from: "src/components/project-files-section.tsx"
      to: "src/components/file-list.tsx"
      via: "FileList component import"
      pattern: "FileList"
---

<objective>
Implement complete UX states across the app: loading indicators, error messages, success feedback, empty states. Also polish the login page and wiki sections.

Purpose: DSGN-04 — a professional tool must communicate system state clearly. Users need feedback when actions are in progress, succeed, or fail.
Output: All interactive components have complete state coverage.
</objective>

<execution_context>
@/Users/jeremyhervo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeremyhervo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/components/file-uploader.tsx
@src/components/file-list.tsx
@src/components/workflow-outputs.tsx
@src/components/project-files-section.tsx
@src/components/wiki-section.tsx
@src/app/login/page.tsx
@src/components/login-form.tsx
@.planning/phases/06-ui-ux-redesign/06-02-SUMMARY.md
@.planning/phases/06-ui-ux-redesign/06-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: UX states for file upload, file list, and workflow outputs</name>
  <files>src/components/file-uploader.tsx, src/components/file-list.tsx, src/components/workflow-outputs.tsx, src/components/project-files-section.tsx</files>
  <action>
**src/components/file-uploader.tsx — uploading state improvements:**

The current uploader already has a loading state but lacks a spinner animation. Improve:

1. Add a CSS spinner instead of just text. Use a pure Tailwind animated spinner:
```tsx
{uploading ? (
  <div className="flex items-center gap-2 text-gray-400">
    <span className="inline-block w-4 h-4 border-2 border-gray-200 border-t-gray-500 rounded-full animate-spin" />
    <span className="text-sm font-medium">Envoi en cours...</span>
  </div>
) : (
  <>
    <span className="text-2xl mb-1">↑</span>
    <span className="text-sm font-medium text-gray-600">Cliquez pour ajouter des fichiers</span>
    <span className="text-xs text-gray-400 mt-0.5">PDF, TXT, MD, DOCX · Max 32MB</span>
  </>
)}
```

2. Error state: change from inline `<p>` to a more prominent error box:
```tsx
{error && (
  <div className="flex items-start gap-2 text-sm text-red-700 bg-red-50 border border-red-200 rounded-lg px-3 py-2.5">
    <span className="shrink-0 mt-0.5">⚠</span>
    <span>{error}</span>
  </div>
)}
```

**src/components/file-list.tsx:**
Read the current file. It shows a list of uploaded files with delete buttons. Improve:

1. Per-file delete loading state: each file's delete button should show a spinner while deleting:
   - Add state tracking for which file is being deleted (already uses a Set, per the history digest)
   - When `deleting.has(path)`: show `<span className="inline-block w-3 h-3 border-2 border-gray-300 border-t-gray-500 rounded-full animate-spin" />` instead of "Supprimer"

2. File item layout: make it a cleaner row with file icon:
```tsx
<li key={file.path} className="flex items-center justify-between py-2.5 px-1">
  <div className="flex items-center gap-2 min-w-0">
    <span className="text-gray-400 shrink-0">📄</span>
    <span className="text-sm text-gray-800 truncate">{file.name}</span>
    {file.metadata?.size && (
      <span className="text-xs text-gray-400 shrink-0">
        {(file.metadata.size / 1024).toFixed(0)} Ko
      </span>
    )}
  </div>
  <button
    onClick={() => handleDelete(file.path)}
    disabled={deleting.has(file.path)}
    className="shrink-0 ml-3 text-xs text-gray-400 hover:text-red-500 disabled:opacity-50 transition-colors"
  >
    {deleting.has(file.path) ? (
      <span className="inline-block w-3 h-3 border-2 border-gray-300 border-t-gray-500 rounded-full animate-spin" />
    ) : "Supprimer"}
  </button>
</li>
```

3. Empty state for file list: if no files, show:
```tsx
<p className="text-sm text-gray-400 py-2 text-center">Aucun fichier ajouté</p>
```

**src/components/workflow-outputs.tsx — empty state + output row polish:**

Empty state (currently just a plain `<p>`):
```tsx
<div className="py-4 text-center">
  <p className="text-sm text-gray-400">Aucun output — complétez un workflow pour voir les résultats ici.</p>
</div>
```

Output row: add a subtle download icon before "Télécharger JSON":
```tsx
<button onClick={() => handleDownload(output)}
  className="shrink-0 text-sm text-blue-600 hover:text-blue-800 font-medium transition-colors flex items-center gap-1">
  ↓ Télécharger
</button>
```

**src/components/project-files-section.tsx:**
Read the current file. It's a stateful wrapper. If there is a loading state for the initial fetch/refresh, add a spinner:
- `isLoading` state: show `<div className="flex justify-center py-4"><span className="inline-block w-5 h-5 border-2 border-gray-200 border-t-gray-500 rounded-full animate-spin" /></div>` while refreshing files.
  </action>
  <verify>
    <automated>cd "/Users/jeremyhervo/Agence Yam Dropbox/Yambox/_IA/getBrandon/webapp" && npm run build 2>&1 | tail -10</automated>
    <manual>File upload shows spinner during upload. File list shows per-file spinner during delete. Outputs empty state shows centered message.</manual>
  </verify>
  <done>File upload/list/outputs have complete UX states (loading spinners, error boxes, empty states). Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Polish login page + login form + wiki sections</name>
  <files>src/app/login/page.tsx, src/components/login-form.tsx, src/components/wiki-section.tsx</files>
  <action>
**src/app/login/page.tsx — layout polish:**
The login page is already centered, but can be improved:
```tsx
export default function LoginPage() {
  return (
    <main className="min-h-screen flex items-center justify-center bg-gray-50">
      <div className="w-full max-w-sm px-4">
        <div className="text-center mb-8">
          <h1 className="text-3xl font-bold text-gray-900 tracking-tight">GET BRANDON</h1>
          <p className="text-sm text-gray-500 mt-2">Outil de production agence</p>
        </div>
        <LoginForm />
      </div>
    </main>
  )
}
```

**src/components/login-form.tsx — form polish + error state:**

Current form uses a basic white card. Improve:

1. Form container: change `p-6 rounded-lg shadow` to `p-7 rounded-xl shadow-sm border border-gray-200`
2. Input fields: change `rounded` to `rounded-lg` on all inputs; add `bg-gray-50 focus:bg-white` for a subtle input background
3. Submit button: increase height `py-2.5`, round to `rounded-lg`
4. Error state: display error in a styled alert box instead of bare `<p>`:
```tsx
{error && (
  <div className="flex items-center gap-2 text-sm text-red-700 bg-red-50 border border-red-100 rounded-lg px-3 py-2.5">
    <span>⚠</span>
    <span>{error}</span>
  </div>
)}
```
5. Loading state on button: add spinner before text:
```tsx
<button type="submit" disabled={loading}
  className="w-full bg-gray-900 text-white py-2.5 px-4 rounded-lg text-sm font-medium hover:bg-gray-800 disabled:opacity-50 transition-colors flex items-center justify-center gap-2">
  {loading && <span className="inline-block w-4 h-4 border-2 border-white/30 border-t-white rounded-full animate-spin" />}
  {loading ? "Connexion..." : "Se connecter"}
</button>
```

**src/components/wiki-section.tsx:**
Read the current file. Improve the visual layout of each wiki section:

1. Section container: add `mb-12` spacing between sections (if not already present), add `scroll-mt-8` (if missing)
2. Section heading: style `<h2>` with `text-2xl font-bold text-gray-900 mb-4 pb-3 border-b border-gray-200`
3. TL;DR box: if there's a TL;DR field in the output_json, highlight it with a distinct background:
```tsx
{tldr && (
  <div className="bg-blue-50 border-l-4 border-blue-400 px-4 py-3 rounded-r-lg mb-6">
    <p className="text-sm font-medium text-blue-800 mb-1">TL;DR</p>
    <p className="text-sm text-blue-900 leading-relaxed">{tldr}</p>
  </div>
)}
```
4. Key-value content rows: read the current rendering logic and ensure each key-value pair has proper spacing: `<dt>` with `text-xs font-semibold text-gray-400 uppercase tracking-wider mb-1` and `<dd>` with `text-sm text-gray-800 leading-relaxed mb-4`.

Note: Read wiki-section.tsx first to understand the current output_json rendering structure before making changes. Preserve all existing logic — only style changes.
  </action>
  <verify>
    <automated>cd "/Users/jeremyhervo/Agence Yam Dropbox/Yambox/_IA/getBrandon/webapp" && npm run build 2>&1 | tail -10</automated>
    <manual>Login page has subtitle, form has rounded-xl container, error shows alert box with icon. Wiki sections have blue TL;DR highlight and styled headings.</manual>
  </verify>
  <done>Login page polished. Login form has error alert + loading spinner. Wiki sections have TL;DR highlight + styled key-value rows. Build passes.</done>
</task>

</tasks>

<verification>
- `npm run build` completes with 0 TypeScript errors and 0 build errors
- File upload: spinner animation during upload, error alert box with icon
- File list: per-file spinner during delete, file size shown, empty state message
- Login: subtitle on page, rounded-xl form card, error alert, spinner on submit
- Wiki: TL;DR highlighted in blue card with left border, section headings with bottom border
</verification>

<success_criteria>
Complete UX state coverage:
- Loading states: file upload spinner, file delete per-item spinner, login submit spinner
- Error states: file upload error alert, login error alert (with icons)
- Empty states: file list empty, workflow outputs empty
- Visual polish: login page subtitle, wiki TL;DR highlighting, wiki section headings
- Build passes: 0 errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-ui-ux-redesign/06-04-SUMMARY.md` with:
- What was implemented (loading states, error states, empty states per component)
- Any rendering logic changes needed in wiki-section.tsx
- Files modified
- Build status
</output>
