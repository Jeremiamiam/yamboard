---
phase: 04-file-upload-output-extraction
plan: "05"
type: execute
wave: 3
depends_on: ["04-02", "04-04"]
files_modified: []
autonomous: false
requirements: [UPLD-01, UPLD-02, UPLD-03, UPLD-04, OUTP-01, OUTP-02, OUTP-03]
must_haves:
  truths:
    - "Production build passes with 0 TypeScript errors"
    - "All Phase 4 success criteria verified in browser by human"
    - "File upload, persistence, list, and delete all work end-to-end"
    - "JSON output extraction, display, and download all work end-to-end"
  artifacts:
    - path: ".planning/phases/04-file-upload-output-extraction/04-05-SUMMARY.md"
      provides: "Phase 4 completion record"
  key_links:
    - from: "Supabase SQL migrations"
      to: "Supabase Dashboard"
      via: "Human must run both migrations before verification"
      pattern: "20260226_project_files.sql, 20260226_workflow_outputs.sql"
---

<objective>
Production build check and full human verification of Phase 4: file upload/list/delete flow and workflow output extraction/display/download flow.

Purpose: Gate against shipping broken functionality. Confirms all 7 Phase 4 requirements (UPLD-01–04, OUTP-01–03) are satisfied from the user's perspective.
Output: Phase 4 marked complete, all requirements verified.
</objective>

<execution_context>
@/Users/jeremyhervo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeremyhervo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-file-upload-output-extraction/04-02-SUMMARY.md
@.planning/phases/04-file-upload-output-extraction/04-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Production build check</name>
  <files></files>
  <action>
    Run the production build to catch any TypeScript or Next.js errors before human verification:

    ```bash
    npx next build 2>&1
    ```

    If the build fails:
    - Fix TypeScript errors reported by the compiler
    - Fix any Next.js build errors (missing exports, invalid routes, etc.)
    - Do NOT proceed to human verification until build passes cleanly

    Also verify:
    - Both migration files exist: `supabase/migrations/20260226_project_files.sql` and `supabase/migrations/20260226_workflow_outputs.sql`
    - Remind in the SUMMARY that the human must run both migrations in Supabase Dashboard before verifying
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -5</automated>
    <manual>Build output ends with "Compiled successfully" or equivalent success indicator. 0 TypeScript errors.</manual>
  </verify>
  <done>Production build passes with 0 errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — full Phase 4 end-to-end</name>
  <action>Pause for human to run both SQL migrations and verify all 7 Phase 4 requirements (UPLD-01–04, OUTP-01–03) in the browser.</action>
  <what-built>
    Phase 4 complete implementation:
    - Supabase Storage `project-files` bucket with RLS (migration: 20260226_project_files.sql)
    - File upload API (POST /api/projects/[id]/files → signed URL), file list API (GET), file delete API (DELETE)
    - FileUploader + FileList + ProjectFilesSection components on project page
    - `workflow_outputs` DB table with RLS (migration: 20260226_workflow_outputs.sql)
    - JSON output extraction in chat route (extractJsonOutput + persistOutput)
    - WorkflowOutputs component on project page with download button
  </what-built>
  <how-to-verify>
    **Prerequisites (run first):**
    1. Run both SQL migrations in Supabase Dashboard → SQL Editor:
       - `supabase/migrations/20260226_project_files.sql`
       - `supabase/migrations/20260226_workflow_outputs.sql`
    2. Start dev server: `npm run dev`

    **File Upload Tests:**
    3. Navigate to any project page
    4. Scroll to "Fichiers" section — verify it appears
    5. Upload a PDF or .txt file — verify it appears in the file list
    6. Reload the page — verify the file still appears (persistence test)
    7. Click "Supprimer" → confirm → verify file disappears from list

    **Output Extraction Tests:**
    8. Scroll to "Outputs" section — verify empty state message when no outputs
    9. Launch a workflow (click "Lancer" on an available workflow)
    10. Chat with the workflow until it produces a JSON output block in the response
    11. Click "Marquer comme terminé" → redirected to project page
    12. Verify the output appears in the "Outputs" section
    13. Click "Télécharger JSON" — verify a .json file is downloaded with valid content

    **All 7 requirements check:**
    - [ ] UPLD-01: Can upload PDF/.txt/.md/.docx
    - [ ] UPLD-02: Files stored in Supabase Storage
    - [ ] UPLD-03: File list visible on project page
    - [ ] UPLD-04: Can delete a file
    - [ ] OUTP-01: JSON output extracted + saved to DB after workflow completion
    - [ ] OUTP-02: Output visible on project page
    - [ ] OUTP-03: Can download JSON output
  </how-to-verify>
  <resume-signal>Type "approved" when all 7 requirements are confirmed, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- `npx next build` passes with 0 errors
- Human verified all 7 Phase 4 requirements (UPLD-01–04, OUTP-01–03)
- Both SQL migrations exist and have been run in Supabase
</verification>

<success_criteria>
- Production build: 0 TypeScript errors, 0 build errors
- All Phase 4 success criteria confirmed by human:
  1. Files (PDF, .txt, .md, .docx) can be uploaded and appear in list
  2. Uploaded files persist in Supabase Storage and survive page reload
  3. Files can be deleted and disappear from list immediately
  4. JSON output visible on project page after workflow completes
  5. JSON output can be downloaded as a file
</success_criteria>

<output>
After completion, create `.planning/phases/04-file-upload-output-extraction/04-05-SUMMARY.md`
</output>
