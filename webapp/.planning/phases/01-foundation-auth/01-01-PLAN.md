---
phase: 01-foundation-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - tailwind.config.ts
  - .env.local
  - .env.example
autonomous: false
requirements: [AUTH-04]

must_haves:
  truths:
    - "Next.js 16 app boots without errors on `npm run dev`"
    - "Supabase env vars are declared and .env.example documents them"
    - "TypeScript compiles without errors"
  artifacts:
    - path: "package.json"
      provides: "Next.js 16 + Supabase dependencies declared"
      contains: "@supabase/ssr"
    - path: ".env.local"
      provides: "Supabase credentials wired to the app"
      contains: "NEXT_PUBLIC_SUPABASE_URL"
    - path: ".env.example"
      provides: "Documentation of required env vars"
  key_links:
    - from: "next.config.ts"
      to: "Node.js runtime"
      via: "no edge runtime override"
      pattern: "nodejs"
---

<objective>
Bootstrap the Next.js 16 project with all required dependencies and Supabase environment configuration.

Purpose: Every subsequent plan depends on a working Next.js 16 app with Supabase credentials available. This creates the foundation.
Output: Runnable Next.js 16 app skeleton with @supabase/ssr installed and env vars wired.
</objective>

<execution_context>
@/Users/jeremyhervo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeremyhervo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-auth/01-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Scaffold Next.js 16 app and install Supabase</name>
  <files>package.json</files>
  <action>
    This step requires running `create-next-app` which is an interactive CLI. The user must execute the scaffold command and install Supabase packages, then provide credentials for Task 2.
    Human must run: `npx create-next-app@latest . --typescript --tailwind --app --src-dir --no-import-alias` inside the WEBAPP directory, then `npm install @supabase/supabase-js @supabase/ssr`.
  </action>
  <verify>
    <automated>MISSING — requires human scaffold step first</automated>
    <manual>Confirm `npm run dev` starts without errors and @supabase/ssr appears in package.json</manual>
  </verify>
  <done>Next.js 16 app scaffolded with @supabase/ssr installed and dev server running</done>
  <what-built>This step requires running `create-next-app` which is an interactive CLI that cannot be fully automated without user input. The user must run the scaffold command and provide Supabase credentials.</what-built>
  <how-to-verify>
    1. Open a terminal in the parent directory of where WEBAPP should live.
    2. Run: `npx create-next-app@latest . --typescript --tailwind --app --src-dir --no-import-alias`
       (Run inside the WEBAPP directory itself, using `.` as the project name to install in place)
    3. When prompted for project name, accept default or type the project name.
    4. After scaffold completes, run: `npm install @supabase/supabase-js @supabase/ssr`
    5. Verify: `npm run dev` starts without errors on http://localhost:3000
    6. Note your Supabase project URL and publishable key from: Supabase Dashboard → Project Settings → API
    7. Type "done" when scaffold is running and you have your Supabase credentials ready.
  </how-to-verify>
  <resume-signal>Type "done" with your SUPABASE_URL and SUPABASE_PUBLISHABLE_KEY values</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Configure env files and next.config.ts</name>
  <files>
    .env.local
    .env.example
    next.config.ts
    .gitignore
  </files>
  <action>
    Create `.env.local` with Supabase credentials (use the values provided by the user in Task 1):
    ```
    NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
    NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your-publishable-key
    ```

    Create `.env.example` documenting required vars:
    ```
    # Supabase — get from: Supabase Dashboard → Project Settings → API
    NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
    NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your-anon-key
    ```

    Update `next.config.ts` to ensure no edge runtime is set globally (Node.js runtime is required for Anthropic SDK streaming in Phase 2). The default config is fine — just ensure there is no `runtime: 'edge'` at the module level:
    ```typescript
    import type { NextConfig } from "next"

    const nextConfig: NextConfig = {
      // Node.js runtime required for Anthropic SDK (Phase 2)
      // Do NOT add experimental.runtime = 'edge' here
    }

    export default nextConfig
    ```

    Verify `.gitignore` already contains `.env.local` (create-next-app includes this). If not, add it.

    Run `npm run build` to verify TypeScript compiles without errors.
  </action>
  <verify>
    <automated>cd "/Users/jeremyhervo/Agence Yam Dropbox/Yambox/_IA/getBrandon/WEBAPP" && npm run build 2>&1 | tail -20</automated>
    <manual>Check that .env.local exists with real Supabase credentials and .env.example is committed (no real secrets).</manual>
  </verify>
  <done>
    - `.env.local` contains NEXT_PUBLIC_SUPABASE_URL and NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY with real values
    - `.env.example` documents both vars with placeholder values
    - `npm run build` completes without TypeScript errors
    - `.gitignore` excludes `.env.local`
  </done>
</task>

</tasks>

<verification>
- `npm run dev` starts on localhost:3000 without errors
- `npm run build` completes successfully
- `.env.local` has both Supabase vars set
- `package.json` contains `@supabase/ssr` and `@supabase/supabase-js` in dependencies
</verification>

<success_criteria>
Next.js 16 app is scaffolded, Supabase packages are installed, and environment is configured. All subsequent plans can reference Supabase env vars and import from `@supabase/ssr`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-01-SUMMARY.md`
</output>
