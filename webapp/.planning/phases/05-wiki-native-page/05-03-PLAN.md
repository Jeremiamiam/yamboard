---
phase: 05-wiki-native-page
plan: "03"
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified: []
autonomous: false
requirements: [WIKI-01, WIKI-02, WIKI-03, WIKI-04]

must_haves:
  truths:
    - "User can visually confirm the wiki page renders correctly for a project with completed workflows"
    - "Scroll-spy highlights sidebar links as user scrolls through sections"
    - "All 4 WIKI requirements are confirmed working end-to-end"
  artifacts:
    - path: "src/app/projects/[id]/wiki/page.tsx"
      provides: "Working wiki route"
  key_links:
    - from: "project page"
      to: "/projects/[id]/wiki"
      via: "Wiki → link in header"
      pattern: "wiki"
---

<objective>
Human verification of the complete wiki feature: page navigation, section filtering, TL;DR display, structured content, and scroll-spy sidebar behavior.

Purpose: Confirm all 4 WIKI requirements work end-to-end in the browser before closing Phase 5.
Output: Approved human checkpoint + Phase 5 complete.
</objective>

<execution_context>
@/Users/jeremyhervo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeremyhervo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build check</name>
  <files></files>
  <action>
    Run TypeScript check and production build to confirm zero errors before human verification.

    ```bash
    cd "/Users/jeremyhervo/Agence Yam Dropbox/Yambox/_IA/getBrandon/webapp"
    npx tsc --noEmit
    npx next build
    ```

    If any TypeScript errors or build failures: fix them before proceeding to the human checkpoint.
    Expected: 0 TS errors, build succeeds with `/projects/[id]/wiki` in route list.
  </action>
  <verify>
    <automated>cd "/Users/jeremyhervo/Agence Yam Dropbox/Yambox/_IA/getBrandon/webapp" && npx tsc --noEmit 2>&1 | head -20 && npx next build 2>&1 | grep -E "(error|✓|Route)" | head -30</automated>
  </verify>
  <done>
    `npx tsc --noEmit` exits 0. `npx next build` succeeds. Route `/projects/[id]/wiki` appears in build output.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — full wiki feature</name>
  <action>
    Pause for human to visually verify the complete wiki feature in the browser. All automated work (Plans 01 and 02) is complete. Human must confirm wiki renders correctly and scroll-spy is functional.
  </action>
  <what-built>
    Complete Phase 5 wiki feature:
    - /projects/[id]/wiki — Server Component wiki page with sidebar + main sections
    - WikiSection component — TL;DR extraction + JSON content per key rendered
    - WikiNav component — scroll-spy sidebar using IntersectionObserver
    - Project page — "Wiki →" link in header
  </what-built>
  <how-to-verify>
    1. Start dev server: `npm run dev` in the webapp directory
    2. Navigate to any project page (e.g. http://localhost:3000/projects/{id})
    3. Confirm "Wiki →" link appears in the project page header (top-right area)
    4. Click "Wiki →" — confirm you land on /projects/[id]/wiki
    5. **If the project has completed workflows with outputs:**
       - Confirm only sections for completed workflows are shown (Contre-brief, Plateforme, Campagne, or Site)
       - Confirm each section shows a blue TL;DR block with an extracted summary string
       - Confirm each section shows the full JSON content as key-value pairs below the TL;DR
       - Confirm the left sidebar lists only the sections that are rendered
       - Scroll down the page — confirm the sidebar link for the currently visible section gets highlighted (blue background + blue text)
    6. **If the project has NO completed workflows:**
       - Confirm the message "Aucun workflow complété — revenez après avoir terminé au moins un workflow." appears
       - Sidebar should be empty (no sections to show)
    7. Check the header shows "← {project name}" link back to the project page — click it to verify navigation works
  </how-to-verify>
  <verify>
    <automated>MISSING — visual browser verification required</automated>
    <manual>See how-to-verify steps above</manual>
  </verify>
  <done>Human types "approved" confirming all verification steps pass</done>
  <resume-signal>Type "approved" if the wiki works as described, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — 0 errors
- `npx next build` — succeeds
- Human verification approved: wiki page renders, scroll-spy works, TL;DR visible, only completed workflow sections shown
</verification>

<success_criteria>
- Build passes with 0 errors
- Human confirms: wiki navigation works, sections filter correctly, TL;DR displays, scroll-spy highlights active section
- Phase 5 complete — all WIKI-01, WIKI-02, WIKI-03, WIKI-04 requirements confirmed
</success_criteria>

<output>
After completion, create `.planning/phases/05-wiki-native-page/05-03-SUMMARY.md`
</output>
