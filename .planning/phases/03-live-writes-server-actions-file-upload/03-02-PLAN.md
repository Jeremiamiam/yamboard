---
phase: 03-live-writes-server-actions-file-upload
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/src/app/(dashboard)/actions/projects.ts
autonomous: true
requirements:
  - PROJECT-1
  - PROJECT-2
  - PROJECT-3
  - PROJECT-4
  - PRODUCT-1
  - PRODUCT-2
  - PRODUCT-3
  - PRODUCT-4
  - PRODUCT-5
  - PERF-2

must_haves:
  truths:
    - "Créer un projet depuis l'UI persiste dans Supabase (lié au bon clientId)"
    - "Éditer un projet met à jour les champs en base"
    - "Supprimer un projet supprime la ligne (cascade sur les produits)"
    - "Ajouter un produit budget associé à un projet persiste en base"
    - "Éditer le montant potentiel d'un projet persiste (remplace ProjectOverrides)"
    - "Mettre à jour une étape de paiement (devis/acompte/avancement/solde) persiste en JSONB"
    - "revalidatePath() est appelé après chaque mutation projet/produit"
  artifacts:
    - path: "dashboard/src/app/(dashboard)/actions/projects.ts"
      provides: "Server Actions CRUD projets + produits budget"
      exports:
        - createProject
        - updateProject
        - deleteProject
        - createBudgetProduct
        - updateBudgetProduct
        - deleteBudgetProduct
        - updatePaymentStage
  key_links:
    - from: "actions/projects.ts"
      to: "supabase.from('projects')"
      via: "createSupabaseClient() dans chaque action"
      pattern: "supabase\\.from\\('projects'\\)"
    - from: "actions/projects.ts"
      to: "supabase.from('budget_products')"
      via: "createSupabaseClient() dans chaque action"
      pattern: "supabase\\.from\\('budget_products'\\)"
---

<objective>
Implémenter les Server Actions CRUD pour les projets et les produits budget, y compris la gestion des étapes de paiement.

Purpose: Les projets et produits sont le coeur de YamBoard. Ces actions remplaceront le contexte `LocalProjects` (projects) et les `localProducts` useState (budget) une fois 03-05 exécuté. L'action `updateProject` avec `potential_amount` remplace définitivement `ProjectOverrides`.
Output: `actions/projects.ts` avec 7 fonctions export couvrant PROJECT-1..4 et PRODUCT-1..5.
</objective>

<execution_context>
@/Users/jeremyhervo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeremyhervo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-live-writes-server-actions-file-upload/03-RESEARCH.md
@dashboard/src/app/login/actions.ts
@dashboard/src/lib/mock.ts
</context>

<interfaces>
<!-- Types disponibles depuis mock.ts -->
```typescript
export type ProjectType = "brand" | "site" | "campaign" | "social" | "other";
export type ProjectStatus = "active" | "done" | "paused" | "draft";

export type Project = {
  id: string;
  clientId: string;
  name: string;
  type: ProjectType;
  status: ProjectStatus;
  description: string;
  progress: number;
  totalPhases: number;
  lastActivity: string;
  startDate: string;
  potentialAmount?: number;
};

export type PaymentStage = {
  amount?: number;
  date?: string;
  status: "pending" | "sent" | "paid";
};

export type BudgetProduct = {
  id: string;
  projectId: string;
  name: string;
  totalAmount: number;
  devis?: PaymentStage;
  acompte?: PaymentStage;
  avancement?: PaymentStage;
  solde?: PaymentStage;
};
```

<!-- Schema SQL -->
```sql
-- projects
CREATE TABLE projects (
  id               UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  client_id        UUID REFERENCES clients(id) ON DELETE CASCADE NOT NULL,
  name             TEXT NOT NULL,
  type             TEXT CHECK (type IN ('brand', 'site', 'campaign', 'social', 'other')),
  status           TEXT CHECK (status IN ('active', 'done', 'paused', 'draft')),
  description      TEXT,
  progress         INT DEFAULT 0,
  total_phases     INT DEFAULT 1,
  last_activity    TEXT,
  start_date       TEXT,
  potential_amount NUMERIC,
  owner_id         UUID REFERENCES auth.users(id) NOT NULL
);

-- budget_products
CREATE TABLE budget_products (
  id           UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  project_id   UUID REFERENCES projects(id) ON DELETE CASCADE NOT NULL,
  name         TEXT NOT NULL,
  total_amount NUMERIC DEFAULT 0,
  devis        JSONB,
  acompte      JSONB,
  avancement   JSONB,
  solde        JSONB,
  owner_id     UUID REFERENCES auth.users(id) NOT NULL
);
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Server Actions projets (createProject, updateProject, deleteProject)</name>
  <files>dashboard/src/app/(dashboard)/actions/projects.ts</files>
  <action>
Créer `dashboard/src/app/(dashboard)/actions/projects.ts` avec `'use server'` et `import 'server-only'` en tête.

**Trois fonctions projet:**

```typescript
// createProject — INSERT dans projects
// Paramètres: clientId + champs projet
// revalidatePath: '/[clientId]' page (liste des projets du client)
export async function createProject(params: {
  clientId: string
  name: string
  type?: ProjectType       // défaut: 'other'
  status?: ProjectStatus   // défaut: 'draft'
  description?: string
  potentialAmount?: number
}): Promise<{ error: string | null; projectId?: string }>
// Retourner { projectId: data.id } au succès — utilisé par ClientPageShell pour naviguer

// updateProject — UPDATE fields arbitraires (inclut potential_amount)
// .eq('id', projectId).eq('owner_id', user.id)
// revalidatePath: '/[clientId]' page + '/[clientId]/[projectId]' page
// IMPORTANT: potential_amount (snake_case en DB) mapped depuis potentialAmount (camelCase dans params)
export async function updateProject(projectId: string, updates: {
  name?: string
  type?: ProjectType
  status?: ProjectStatus
  description?: string
  potentialAmount?: number  // → potential_amount en DB
  progress?: number
}): Promise<{ error: string | null }>

// deleteProject — DELETE row (cascade supprime les budget_products et documents liés)
// Fetch client_id AVANT suppression pour revalidatePath
// revalidatePath: '/' + '/[clientId]' page
export async function deleteProject(projectId: string): Promise<{ error: string | null }>
```

Règles identiques: `getUser()`, `owner_id: user.id`, `.eq('owner_id', user.id)` sur UPDATE/DELETE, `revalidatePath()` après mutation.
  </action>
  <verify>
    <automated>cd dashboard && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
- 3 fonctions projet exportées dans `actions/projects.ts`
- TypeScript compile
- `deleteProject` fetch `client_id` avant de supprimer pour alimenter `revalidatePath`
  </done>
</task>

<task type="auto">
  <name>Task 2: Server Actions produits budget (createBudgetProduct, updateBudgetProduct, deleteBudgetProduct, updatePaymentStage)</name>
  <files>dashboard/src/app/(dashboard)/actions/projects.ts</files>
  <action>
Ajouter 4 fonctions dans le même fichier `actions/projects.ts` (budget_products sont co-localisés avec les projets).

**Quatre fonctions budget_products:**

```typescript
// createBudgetProduct — INSERT dans budget_products
// revalidatePath: '/[clientId]/[projectId]' page + '/compta' page
// Fetch project row first (pour récupérer client_id → revalidatePath client)
export async function createBudgetProduct(params: {
  projectId: string
  name: string
  totalAmount?: number  // défaut: 0
}): Promise<{ error: string | null; productId?: string }>

// updateBudgetProduct — UPDATE name + total_amount
// .eq('id', productId).eq('owner_id', user.id)
// revalidatePath: '/[clientId]/[projectId]' page + '/compta' page
export async function updateBudgetProduct(productId: string, updates: {
  name?: string
  totalAmount?: number  // → total_amount en DB
}): Promise<{ error: string | null }>

// deleteBudgetProduct — DELETE row
// revalidatePath: '/[clientId]/[projectId]' page + '/compta' page
export async function deleteBudgetProduct(productId: string): Promise<{ error: string | null }>

// updatePaymentStage — UPDATE colonne JSONB spécifique (devis/acompte/avancement/solde)
// Paramètre stage: 'devis' | 'acompte' | 'avancement' | 'solde'
// La valeur PaymentStage est stockée directement en JSONB
// .eq('id', productId).eq('owner_id', user.id)
// revalidatePath: '/[clientId]/[projectId]' page + '/compta' page
export async function updatePaymentStage(
  productId: string,
  stage: 'devis' | 'acompte' | 'avancement' | 'solde',
  value: PaymentStage
): Promise<{ error: string | null }>
```

Pour `updatePaymentStage`, construire l'objet de mise à jour dynamiquement:
```typescript
const { error } = await supabase
  .from('budget_products')
  .update({ [stage]: value })
  .eq('id', productId)
  .eq('owner_id', user.id)
```

Note: Pour `createBudgetProduct` et `deleteBudgetProduct`, fetch le `project_id → client_id` en amont pour construire les paths `revalidatePath` corrects (URL browser = `/[clientId]/[projectId]`).
  </action>
  <verify>
    <automated>cd dashboard && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
- 7 fonctions totales exportées dans `actions/projects.ts` (3 projet + 4 budget)
- TypeScript compile sans erreur
- `updatePaymentStage` utilise un paramètre `stage` dynamique comme clé JSONB
- `revalidatePath('/compta', 'page')` présent dans les mutations budget_products
  </done>
</task>

</tasks>

<verification>
```bash
# TypeScript compile
cd dashboard && npx tsc --noEmit 2>&1 | head -20

# Vérifier les 7 exports
grep -E "^export async function (createProject|updateProject|deleteProject|createBudgetProduct|updateBudgetProduct|deleteBudgetProduct|updatePaymentStage)" dashboard/src/app/\(dashboard\)/actions/projects.ts

# Vérifier revalidatePath('/compta')
grep "revalidatePath.*compta" dashboard/src/app/\(dashboard\)/actions/projects.ts
```
</verification>

<success_criteria>
- `actions/projects.ts` exporte les 7 fonctions avec `'use server'` en tête
- TypeScript compile (`cd dashboard && npx tsc --noEmit` exit 0)
- `updateProject` mappe `potentialAmount → potential_amount` avant l'UPDATE
- `updatePaymentStage` utilise le `stage` comme clé dynamique dans l'objet UPDATE
- `revalidatePath('/compta', 'page')` présent dans les mutations budget_products
</success_criteria>

<output>
After completion, create `.planning/phases/03-live-writes-server-actions-file-upload/03-02-SUMMARY.md`
</output>
