---
phase: 01-foundation-auth-infrastructure-schema
plan: "03"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - dashboard/src/middleware.ts
  - dashboard/src/app/login/page.tsx
  - dashboard/src/app/login/actions.ts
  - dashboard/src/app/(dashboard)/layout.tsx
autonomous: false
requirements:
  - AUTH-1
  - AUTH-2
  - AUTH-3
  - AUTH-4

must_haves:
  truths:
    - "Naviguer vers / sans session redirige vers /login"
    - "Soumettre des credentials valides depuis /login redirige vers /"
    - "Soumettre des credentials invalides affiche un message d'erreur sur /login"
    - "Logout redirige vers /login et invalide la session"
    - "Le middleware s'exécute sur chaque requête et rafraîchit le token via getUser()"
    - "Aucun appel getSession() dans middleware.ts, layout.tsx ou actions.ts"
  artifacts:
    - path: "dashboard/src/middleware.ts"
      provides: "Token refresh sur chaque requête + exclusion de /login du matcher"
      contains: "supabase.auth.getUser()"
    - path: "dashboard/src/app/login/page.tsx"
      provides: "Formulaire email + mot de passe"
    - path: "dashboard/src/app/login/actions.ts"
      provides: "Server Actions login() + logout()"
      exports: ["login", "logout"]
    - path: "dashboard/src/app/(dashboard)/layout.tsx"
      provides: "Protected layout — redirect /login si user null"
      contains: "if (!user) redirect('/login')"
  key_links:
    - from: "dashboard/src/app/login/actions.ts login()"
      to: "dashboard/src/lib/supabase/server.ts createClient()"
      via: "import { createClient } from '@/lib/supabase/server'"
      pattern: "createClient"
    - from: "dashboard/src/app/(dashboard)/layout.tsx"
      to: "dashboard/src/lib/supabase/server.ts createClient()"
      via: "supabase.auth.getUser()"
      pattern: "getUser"
    - from: "dashboard/src/middleware.ts"
      to: "@supabase/ssr createServerClient"
      via: "supabase.auth.getUser()"
      pattern: "getUser"
---

<objective>
Créer le middleware de refresh de token, la page de login, les Server Actions d'authentification et le layout protégé. Toutes les routes existantes seront protégées par le layout (dashboard) sans aucune modification UI.

Purpose: Rendre l'application inaccessible sans authentification — prérequis à toute donnée réelle en Phase 02.
Output: middleware.ts, login/page.tsx, login/actions.ts, (dashboard)/layout.tsx
</objective>

<execution_context>
@/Users/jeremyhervo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeremyhervo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/01-foundation-auth-infrastructure-schema/01-01-SUMMARY.md
</context>

<interfaces>
<!-- Contrats fournis par Plan 01-01 (dashboard/src/lib/supabase/) -->

From dashboard/src/lib/supabase/server.ts:
```typescript
// Per-request server client — async, utiliser avec await
export async function createClient(): Promise<SupabaseClient>
// Usage: const supabase = await createClient()
// Usage: const { data: { user } } = await supabase.auth.getUser()
```

From dashboard/src/lib/supabase/client.ts:
```typescript
// Browser client singleton — pour Client Components uniquement
export function createClient(): SupabaseClient
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Créer middleware.ts + Server Actions auth</name>
  <files>
    dashboard/src/middleware.ts,
    dashboard/src/app/login/actions.ts
  </files>
  <action>
**dashboard/src/middleware.ts** (à la racine de src/, PAS à l'intérieur de app/) :

```typescript
import { createServerClient } from '@supabase/ssr'
import { NextResponse, type NextRequest } from 'next/server'

export async function middleware(request: NextRequest) {
  let supabaseResponse = NextResponse.next({ request })

  const supabase = createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        getAll() {
          return request.cookies.getAll()
        },
        setAll(cookiesToSet) {
          cookiesToSet.forEach(({ name, value }) =>
            request.cookies.set(name, value)
          )
          supabaseResponse = NextResponse.next({ request })
          cookiesToSet.forEach(({ name, value, options }) =>
            supabaseResponse.cookies.set(name, value, options)
          )
        },
      },
    }
  )

  // CRITIQUE : getUser() — jamais getSession() — c'est ce qui valide réellement le token côté serveur
  await supabase.auth.getUser()

  return supabaseResponse
}

export const config = {
  matcher: [
    '/((?!login|_next/static|_next/image|favicon.ico|.*\\.(?:svg|png|jpg|jpeg|gif|webp)$).*)',
  ],
}
```

Règle critique : Le matcher exclut /login pour éviter les boucles infinies de redirection 302.

**dashboard/src/app/login/actions.ts** :

```typescript
'use server'

import { createClient } from '@/lib/supabase/server'
import { redirect } from 'next/navigation'

export async function login(formData: FormData) {
  const supabase = await createClient()

  const { error } = await supabase.auth.signInWithPassword({
    email: formData.get('email') as string,
    password: formData.get('password') as string,
  })

  if (error) {
    return { error: error.message }
  }

  redirect('/')
}

export async function logout() {
  const supabase = await createClient()
  await supabase.auth.signOut()
  redirect('/login')
}
```

NE PAS utiliser getSession() dans ces actions — uniquement createClient() puis les méthodes d'auth directes.
  </action>
  <verify>
    <automated>ls /Users/jeremyhervo/Agence\ Yam\ Dropbox/Yambox/_IA/getBrandon/getBrandon/dashboard/src/middleware.ts /Users/jeremyhervo/Agence\ Yam\ Dropbox/Yambox/_IA/getBrandon/getBrandon/dashboard/src/app/login/actions.ts && grep -c "getUser" /Users/jeremyhervo/Agence\ Yam\ Dropbox/Yambox/_IA/getBrandon/getBrandon/dashboard/src/middleware.ts</automated>
  </verify>
  <done>
- dashboard/src/middleware.ts existe à dashboard/src/ (pas dans app/)
- middleware.ts appelle supabase.auth.getUser() (pas getSession())
- Le matcher exclut /login
- dashboard/src/app/login/actions.ts exporte login() et logout() avec 'use server'
  </done>
</task>

<task type="auto">
  <name>Task 2: Créer la page de login + le layout protégé (dashboard)</name>
  <files>
    dashboard/src/app/login/page.tsx,
    dashboard/src/app/(dashboard)/layout.tsx
  </files>
  <action>
**dashboard/src/app/login/page.tsx** :

Page Client Component avec formulaire email/mot de passe appelant la Server Action login().

```typescript
'use client'

import { login } from './actions'
import { useState } from 'react'
import { useFormStatus } from 'react-dom'

function SubmitButton() {
  const { pending } = useFormStatus()
  return (
    <button
      type="submit"
      disabled={pending}
      className="w-full py-2 px-4 bg-zinc-800 hover:bg-zinc-700 text-white rounded-lg transition-colors disabled:opacity-50 disabled:cursor-not-allowed"
    >
      {pending ? 'Connexion...' : 'Se connecter'}
    </button>
  )
}

export default function LoginPage() {
  const [error, setError] = useState<string | null>(null)

  async function handleSubmit(formData: FormData) {
    setError(null)
    const result = await login(formData)
    if (result?.error) {
      setError(result.error)
    }
  }

  return (
    <div className="min-h-screen flex items-center justify-center bg-zinc-950">
      <div className="w-full max-w-sm space-y-6 p-8">
        <div className="space-y-1">
          <h1 className="text-xl font-semibold text-white">YamBoard</h1>
          <p className="text-sm text-zinc-400">Connectez-vous pour continuer</p>
        </div>

        <form action={handleSubmit} className="space-y-4">
          <div className="space-y-1">
            <label htmlFor="email" className="text-sm text-zinc-400">
              Email
            </label>
            <input
              id="email"
              name="email"
              type="email"
              required
              autoComplete="email"
              className="w-full py-2 px-3 bg-zinc-900 border border-zinc-700 rounded-lg text-white placeholder-zinc-500 focus:outline-none focus:ring-1 focus:ring-zinc-500"
              placeholder="votre@email.com"
            />
          </div>

          <div className="space-y-1">
            <label htmlFor="password" className="text-sm text-zinc-400">
              Mot de passe
            </label>
            <input
              id="password"
              name="password"
              type="password"
              required
              autoComplete="current-password"
              className="w-full py-2 px-3 bg-zinc-900 border border-zinc-700 rounded-lg text-white placeholder-zinc-500 focus:outline-none focus:ring-1 focus:ring-zinc-500"
              placeholder="••••••••"
            />
          </div>

          {error && (
            <p className="text-sm text-red-400">{error}</p>
          )}

          <SubmitButton />
        </form>
      </div>
    </div>
  )
}
```

**dashboard/src/app/(dashboard)/layout.tsx** :

Route group — les parenthèses dans (dashboard) n'ajoutent aucun segment d'URL. Les routes existantes [clientId]/, api/, compta/ doivent être déplacées dans ce dossier pour être protégées. Le root layout.tsx existant (avec les providers) reste inchangé.

```typescript
import { createClient } from '@/lib/supabase/server'
import { redirect } from 'next/navigation'

export default async function DashboardLayout({
  children,
}: {
  children: React.ReactNode
}) {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()

  if (!user) redirect('/login')

  return <>{children}</>
}
```

**Action requise — déplacer les routes existantes dans (dashboard)/ :**

Les routes suivantes doivent être déplacées pour être protégées par ce layout :
- dashboard/src/app/[clientId]/ → dashboard/src/app/(dashboard)/[clientId]/
- dashboard/src/app/api/ → dashboard/src/app/(dashboard)/api/
- dashboard/src/app/compta/ → dashboard/src/app/(dashboard)/compta/
- dashboard/src/app/page.tsx → dashboard/src/app/(dashboard)/page.tsx

La route dashboard/src/app/layout.tsx (root layout avec providers ThemeProvider, LocalProjectsProvider, etc.) reste à sa place — elle n'est PAS déplacée dans (dashboard)/.

Vérifier après déplacement que npm run dev démarre sans erreur de routing.
  </action>
  <verify>
    <automated>ls /Users/jeremyhervo/Agence\ Yam\ Dropbox/Yambox/_IA/getBrandon/getBrandon/dashboard/src/app/login/page.tsx /Users/jeremyhervo/Agence\ Yam\ Dropbox/Yambox/_IA/getBrandon/getBrandon/dashboard/src/app/\(dashboard\)/layout.tsx && grep -c "redirect" /Users/jeremyhervo/Agence\ Yam\ Dropbox/Yambox/_IA/getBrandon/getBrandon/dashboard/src/app/\(dashboard\)/layout.tsx</automated>
  </verify>
  <done>
- dashboard/src/app/login/page.tsx existe avec formulaire email + mot de passe
- dashboard/src/app/(dashboard)/layout.tsx existe et appelle getUser() + redirect('/login') si !user
- Routes existantes ([clientId]/, api/, compta/, page.tsx) déplacées dans (dashboard)/
- npm run dev démarre sans erreur
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Auth gate complet : middleware de refresh + page de login + protected layout.
    Toutes les routes dashboard sont maintenant dans (dashboard)/ et protégées.
  </what-built>
  <how-to-verify>
    1. Démarrer le serveur : cd dashboard && npm run dev
    2. Ouvrir http://localhost:3000 dans un navigateur sans session active
       → Doit rediriger vers http://localhost:3000/login
    3. Soumettre des credentials invalides (ex: test@test.com / wrongpassword)
       → Doit afficher un message d'erreur sur la page login
    4. Soumettre des credentials valides (créés dans Supabase Dashboard > Authentication > Users)
       → Doit rediriger vers http://localhost:3000 (page principale)
    5. Une fois connecté, vérifier que la page principale se charge correctement (UI v2 intacte)
    6. Tester le logout depuis l'UI (ou via un bouton temporaire) → doit rediriger vers /login
    7. Après logout, retenter /login → doit afficher le formulaire (pas de boucle infinie)
  </how-to-verify>
  <resume-signal>Taper "approved" ou décrire les problèmes rencontrés</resume-signal>
</task>

</tasks>

<verification>
```bash
# Fichiers créés
ls dashboard/src/middleware.ts
ls dashboard/src/app/login/page.tsx
ls dashboard/src/app/login/actions.ts
ls dashboard/src/app/(dashboard)/layout.tsx

# SEC-3 : aucun getSession() dans les fichiers serveur
grep -r "getSession" dashboard/src --include="*.ts" --include="*.tsx"
# Attendu : 0 résultats (ou uniquement dans des fichiers 'use client')

# Routes déplacées dans (dashboard)/
ls dashboard/src/app/(dashboard)/

# Build sans erreur
cd dashboard && npm run build 2>&1 | tail -20
```
</verification>

<success_criteria>
- Naviguer vers / sans session redirige vers /login (AUTH-2)
- Login avec credentials valides redirige vers / (AUTH-1)
- Login avec credentials invalides affiche l'erreur Supabase (AUTH-1)
- Logout redirige vers /login (AUTH-4)
- Aucun appel getSession() dans les fichiers serveur (SEC-3)
- npm run dev sans erreur
</success_criteria>

<output>
Après complétion, créer `.planning/phases/01-foundation-auth-infrastructure-schema/01-03-SUMMARY.md`
</output>
