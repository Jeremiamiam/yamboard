---
phase: 01-foundation-auth-infrastructure-schema
plan: "05"
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
  - "01-03"
files_modified:
  - dashboard/src/middleware.ts
  - dashboard/src/lib/supabase/server.ts
  - dashboard/src/app/login/actions.ts
  - dashboard/src/app/(dashboard)/layout.tsx
  - dashboard/supabase/migrations/002_rls.sql
autonomous: false
requirements:
  - SEC-1
  - SEC-2
  - SEC-3
  - SEC-4

must_haves:
  truths:
    - "grep 'getSession' dashboard/src retourne 0 résultats dans les fichiers non-client"
    - "Chaque politique INSERT et UPDATE dans 002_rls.sql a un WITH CHECK"
    - "SUPABASE_SERVICE_ROLE_KEY n'apparaît jamais avec le préfixe NEXT_PUBLIC_"
    - "Tous les fichiers qui importent le service role key ont 'import server-only' ou sont dans server.ts"
    - "Toutes les 5 tables ont RLS activé et au moins une politique dans pg_policies"
    - "La requête pg_policies WHERE cmd IN (INSERT, UPDATE) AND with_check IS NULL retourne 0 lignes"
  artifacts:
    - path: "dashboard/src/lib/supabase/server.ts"
      provides: "Server-only client — validé sans getSession() et sans NEXT_PUBLIC_ service key"
    - path: "dashboard/supabase/migrations/002_rls.sql"
      provides: "20 politiques RLS avec WITH CHECK sur tous les INSERT/UPDATE — auditées et validées"
  key_links:
    - from: "audit grep getSession"
      to: "dashboard/src/middleware.ts + dashboard/src/app/(dashboard)/layout.tsx + dashboard/src/app/login/actions.ts"
      via: "grep -r getSession dashboard/src --include=*.ts --include=*.tsx"
      pattern: "getSession"
    - from: "audit NEXT_PUBLIC_ service key"
      to: "dashboard/.env.local + dashboard/.env.example + tous les .ts"
      via: "grep -r NEXT_PUBLIC_SUPABASE_SERVICE dashboard/"
      pattern: "NEXT_PUBLIC_SUPABASE_SERVICE"
---

<objective>
Effectuer l'audit de sécurité complet de Phase 01 : vérifier que getUser() est utilisé partout côté serveur, que WITH CHECK est présent sur tous les INSERT/UPDATE, et que le service role key n'est jamais exposé publiquement. Corriger tout problème trouvé.

Purpose: Les failles de sécurité auth installées en Phase 01 sont difficiles à auditer après 18 plans. Ce plan ferme la boucle de sécurité avant d'avancer vers la Phase 02.
Output: Rapport d'audit avec corrections appliquées, prêt pour le checkpoint de vérification humaine.
</objective>

<execution_context>
@/Users/jeremyhervo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeremyhervo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/01-foundation-auth-infrastructure-schema/01-01-SUMMARY.md
@.planning/phases/01-foundation-auth-infrastructure-schema/01-02-SUMMARY.md
@.planning/phases/01-foundation-auth-infrastructure-schema/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit SEC-3 et SEC-4 — grep côté fichiers TypeScript</name>
  <files>
    dashboard/src/middleware.ts,
    dashboard/src/lib/supabase/server.ts,
    dashboard/src/app/login/actions.ts,
    dashboard/src/app/(dashboard)/layout.tsx
  </files>
  <action>
Exécuter les commandes d'audit suivantes et corriger immédiatement tout problème détecté.

**Audit SEC-3 — aucun getSession() côté serveur :**

```bash
grep -r "getSession" dashboard/src --include="*.ts" --include="*.tsx"
```

Résultat attendu : 0 lignes.

Si des résultats apparaissent, vérifier si le fichier contient 'use client' en première ligne :
- Si 'use client' : acceptable — getSession() est autorisé dans les Client Components
- Si pas 'use client' : problème de sécurité — remplacer getSession() par getUser()

Les fichiers à vérifier en priorité (ne doivent jamais appeler getSession()) :
- dashboard/src/middleware.ts
- dashboard/src/lib/supabase/server.ts
- dashboard/src/app/(dashboard)/layout.tsx
- dashboard/src/app/login/actions.ts
- Tout fichier Server Component ou Server Action

**Audit SEC-4 — service role key jamais préfixée NEXT_PUBLIC_ :**

```bash
grep -r "NEXT_PUBLIC_SUPABASE_SERVICE" dashboard/
```

Résultat attendu : 0 lignes. Si des résultats apparaissent, corriger immédiatement — supprimer le préfixe NEXT_PUBLIC_ et vérifier que le fichier utilise uniquement cette variable côté serveur.

```bash
grep -r "SERVICE_ROLE" dashboard/src --include="*.ts" --include="*.tsx"
```

Si des résultats apparaissent, vérifier que chaque fichier contient soit :
- `import 'server-only'` en première ligne, OU
- Est un fichier avec 'use server', OU
- Est dashboard/src/lib/supabase/server.ts (accepté, fichier serveur pur)

Si le service role key n'est pas encore utilisé dans Phase 01, l'audit doit confirmer 0 résultats (normal — il sera utilisé en Phase 03 pour le Storage admin).

**Corriger tout problème détecté avant de passer à Task 2.**

Produire un résumé des résultats d'audit sous forme de commentaire dans le fichier SUMMARY.md après complétion.
  </action>
  <verify>
    <automated>grep -r "getSession" /Users/jeremyhervo/Agence\ Yam\ Dropbox/Yambox/_IA/getBrandon/getBrandon/dashboard/src --include="*.ts" --include="*.tsx" | grep -v "use client" | wc -l && grep -r "NEXT_PUBLIC_SUPABASE_SERVICE" /Users/jeremyhervo/Agence\ Yam\ Dropbox/Yambox/_IA/getBrandon/getBrandon/dashboard/ | wc -l</automated>
  </verify>
  <done>
- grep getSession dans les fichiers non-'use client' retourne 0 lignes (SEC-3)
- grep NEXT_PUBLIC_SUPABASE_SERVICE retourne 0 lignes (SEC-4)
- Tout fichier utilisant SERVICE_ROLE_KEY a 'import server-only' ou est un fichier 'use server'
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit SEC-1 et SEC-2 — vérifier les fichiers SQL</name>
  <files>dashboard/supabase/migrations/002_rls.sql</files>
  <action>
Auditer 002_rls.sql pour confirmer la conformité SEC-1 (RLS sur toutes les tables) et SEC-2 (WITH CHECK sur tous les INSERT/UPDATE).

**Audit SEC-2 — WITH CHECK sur tous les INSERT et UPDATE :**

Compter les politiques INSERT et UPDATE dans 002_rls.sql :

```bash
grep -c "FOR INSERT" dashboard/supabase/migrations/002_rls.sql
# Attendu : 5 (une par table)

grep -c "FOR UPDATE" dashboard/supabase/migrations/002_rls.sql
# Attendu : 5 (une par table)

grep -c "WITH CHECK" dashboard/supabase/migrations/002_rls.sql
# Attendu : 10 (chaque INSERT + chaque UPDATE a WITH CHECK)
```

Si le compte de WITH CHECK est inférieur à 10, identifier les politiques manquantes et ajouter WITH CHECK.

Vérification croisée : le nombre de WITH CHECK (10) doit être égal à la somme des FOR INSERT (5) + FOR UPDATE (5).

**Audit SEC-1 — ENABLE ROW LEVEL SECURITY sur toutes les tables :**

```bash
grep -c "ENABLE ROW LEVEL SECURITY" dashboard/supabase/migrations/002_rls.sql
# Attendu : 5 (clients, contacts, projects, budget_products, documents)
```

**Audit ARCH-6 — 4 fichiers de migration présents :**

```bash
ls dashboard/supabase/migrations/
# Attendu : 001_schema.sql  002_rls.sql  003_indexes.sql  004_seed.sql
```

**Si des problèmes sont détectés dans 002_rls.sql** : corriger le fichier en ajoutant les clauses manquantes. Le fichier doit rester exécutable dans Supabase Studio (pas de modifications partielles — toute la table doit avoir ses 4 politiques complètes).

**Requêtes SQL de validation (à exécuter dans Supabase Studio après exécution des migrations) :**

Documenter ces requêtes dans le fichier SUMMARY.md pour que l'utilisateur puisse les exécuter :

```sql
-- SEC-1 : toutes les tables ont au moins une politique
SELECT tablename FROM pg_tables
WHERE schemaname = 'public'
AND tablename NOT IN (
  SELECT DISTINCT tablename FROM pg_policies
);
-- Attendu : 0 lignes

-- SEC-2 : aucun INSERT/UPDATE sans WITH CHECK
SELECT policyname, tablename, cmd, with_check
FROM pg_policies
WHERE cmd IN ('INSERT', 'UPDATE') AND with_check IS NULL;
-- Attendu : 0 lignes

-- PERF-1 : 10 index présents
SELECT indexname, tablename FROM pg_indexes
WHERE tablename IN ('clients','contacts','projects','budget_products','documents')
ORDER BY tablename;
-- Attendu : 10 lignes

-- Vue complète des politiques (validation finale)
SELECT tablename, policyname, cmd FROM pg_policies ORDER BY tablename, cmd;
-- Attendu : 20 lignes (4 politiques × 5 tables)
```
  </action>
  <verify>
    <automated>grep -c "WITH CHECK" /Users/jeremyhervo/Agence\ Yam\ Dropbox/Yambox/_IA/getBrandon/getBrandon/dashboard/supabase/migrations/002_rls.sql && grep -c "ENABLE ROW LEVEL SECURITY" /Users/jeremyhervo/Agence\ Yam\ Dropbox/Yambox/_IA/getBrandon/getBrandon/dashboard/supabase/migrations/002_rls.sql</automated>
  </verify>
  <done>
- grep -c "WITH CHECK" retourne 10 (SEC-2)
- grep -c "ENABLE ROW LEVEL SECURITY" retourne 5 (SEC-1)
- ls dashboard/supabase/migrations/ affiche les 4 fichiers (ARCH-6)
- Requêtes SQL de validation documentées dans SUMMARY.md pour exécution dans Supabase Studio
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Audit de sécurité complet de Phase 01.
    Task 1 a vérifié getSession() et SERVICE_ROLE_KEY via grep.
    Task 2 a vérifié WITH CHECK et ENABLE ROW LEVEL SECURITY dans les fichiers SQL.
  </what-built>
  <how-to-verify>
    Exécuter les requêtes SQL suivantes dans Supabase Studio > SQL Editor APRÈS avoir exécuté les migrations 001 → 002 → 003 :

    1. SEC-1 — toutes les tables ont des politiques :
       ```sql
       SELECT tablename FROM pg_tables
       WHERE schemaname = 'public'
       AND tablename NOT IN (SELECT DISTINCT tablename FROM pg_policies);
       ```
       Attendu : 0 lignes retournées.

    2. SEC-2 — aucun INSERT/UPDATE sans WITH CHECK :
       ```sql
       SELECT policyname, tablename, cmd, with_check FROM pg_policies
       WHERE cmd IN ('INSERT', 'UPDATE') AND with_check IS NULL;
       ```
       Attendu : 0 lignes retournées.

    3. PERF-1 — index présents :
       ```sql
       SELECT indexname, tablename FROM pg_indexes
       WHERE tablename IN ('clients','contacts','projects','budget_products','documents')
       ORDER BY tablename;
       ```
       Attendu : 10 lignes retournées.

    4. Comptage des politiques (20 total) :
       ```sql
       SELECT COUNT(*) FROM pg_policies
       WHERE tablename IN ('clients','contacts','projects','budget_products','documents');
       ```
       Attendu : 20.

    5. Vérification des migrations SQL présentes (depuis le terminal) :
       ```bash
       ls dashboard/supabase/migrations/
       ```
       Attendu : 001_schema.sql  002_rls.sql  003_indexes.sql  004_seed.sql

    Si toutes les requêtes retournent les résultats attendus, Phase 01 est sécurisée et prête pour Phase 02.
  </how-to-verify>
  <resume-signal>Taper "approved" si toutes les vérifications sont vertes, ou décrire les résultats inattendus</resume-signal>
</task>

</tasks>

<verification>
```bash
# SEC-3 — aucun getSession() côté serveur
grep -r "getSession" dashboard/src --include="*.ts" --include="*.tsx" | grep -v "use client"
# Attendu : 0 résultats

# SEC-4 — service role key jamais NEXT_PUBLIC_
grep -r "NEXT_PUBLIC_SUPABASE_SERVICE" dashboard/
# Attendu : 0 résultats

# SEC-2 — WITH CHECK sur tous les INSERT/UPDATE
grep -c "WITH CHECK" dashboard/supabase/migrations/002_rls.sql
# Attendu : 10

# SEC-1 — RLS activé sur toutes les tables
grep -c "ENABLE ROW LEVEL SECURITY" dashboard/supabase/migrations/002_rls.sql
# Attendu : 5

# ARCH-6 — 4 fichiers de migration
ls dashboard/supabase/migrations/ | wc -l
# Attendu : 4
```
</verification>

<success_criteria>
- SEC-3 : 0 appels getSession() dans les fichiers serveur
- SEC-4 : 0 occurrences de NEXT_PUBLIC_SUPABASE_SERVICE dans tout le projet
- SEC-2 : 10 WITH CHECK dans 002_rls.sql (tous les INSERT + UPDATE)
- SEC-1 : 5 ENABLE ROW LEVEL SECURITY dans 002_rls.sql
- Checkpoint humain approuvé après validation des requêtes pg_policies dans Supabase Studio
</success_criteria>

<output>
Après complétion, créer `.planning/phases/01-foundation-auth-infrastructure-schema/01-05-SUMMARY.md`
</output>
