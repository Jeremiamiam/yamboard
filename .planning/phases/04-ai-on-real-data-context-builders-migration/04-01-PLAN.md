---
phase: 04-ai-on-real-data-context-builders-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/src/lib/types.ts
  - dashboard/src/lib/data/clients.ts
  - dashboard/src/lib/data/projects.ts
  - dashboard/src/lib/data/documents.ts
  - dashboard/src/app/(dashboard)/actions/documents.ts
  - dashboard/src/app/(dashboard)/actions/clients.ts
  - dashboard/src/app/(dashboard)/actions/projects.ts
  - dashboard/src/hooks/useChat.ts
  - dashboard/src/components/ClientSidebar.tsx
  - dashboard/src/components/ProductDrawer.tsx
  - dashboard/src/components/ProjectPageShell.tsx
  - dashboard/src/components/ClientPageShell.tsx
  - dashboard/src/components/DocumentViewer.tsx
  - dashboard/src/components/AddDocForm.tsx
  - dashboard/src/components/tabs/ChatTab.tsx
  - dashboard/src/components/tabs/ClientChatTab.tsx
  - dashboard/src/components/tabs/ProduitsTab.tsx
  - dashboard/src/components/tabs/BudgetsTab.tsx
  - dashboard/src/components/tabs/DocumentsTab.tsx
  - dashboard/src/app/(dashboard)/compta/page.tsx
autonomous: true
requirements: [AI-1, AI-2, AI-3, AI-4, AI-5, AI-6, ARCH-5]

must_haves:
  truths:
    - "All type imports compile from @/lib/types (not @/lib/mock)"
    - "UI config maps (DOC_TYPE_LABEL, PAYMENT_STAGE_LABEL, PROJECT_STATUS_CONFIG, DOC_TYPE_COLOR) exist in lib/types.ts"
    - "mock.ts runtime values (CLIENTS, PROJECTS, etc.) are NOT in lib/types.ts"
    - "npm run build passes with zero TypeScript errors"
  artifacts:
    - path: "dashboard/src/lib/types.ts"
      provides: "All type exports and UI config maps migrated from mock.ts"
      exports: ["Client", "Project", "Document", "BudgetProduct", "PaymentStage", "Message", "Conversation", "ClientCategory", "ClientStatus", "ProjectType", "ProjectStatus", "DOC_TYPE_LABEL", "DOC_TYPE_COLOR", "PAYMENT_STAGE_LABEL", "PROJECT_STATUS_CONFIG"]
  key_links:
    - from: "dashboard/src/lib/data/clients.ts"
      to: "dashboard/src/lib/types.ts"
      via: "import type { Client, ClientCategory, ClientStatus }"
      pattern: "from '@/lib/types'"
    - from: "dashboard/src/components/DocumentViewer.tsx"
      to: "dashboard/src/lib/types.ts"
      via: "import { DOC_TYPE_LABEL, DOC_TYPE_COLOR }"
      pattern: "from '@/lib/types'"
---

<objective>
Create `lib/types.ts` as the new home for all shared types and UI config maps, then migrate every `from '@/lib/mock'` import across 20 files to `from '@/lib/types'`.

Purpose: Prerequisite for deleting `mock.ts` at end of phase. All downstream plans (04-02 through 04-05) need the types available in `lib/types.ts`. The runtime mock data arrays (CLIENTS, PROJECTS, etc.) stay in `mock.ts` until plan 04-05.

Output: `lib/types.ts` with all type/config exports. Every file that previously imported types from `@/lib/mock` now imports from `@/lib/types`. Build passes clean.
</objective>

<execution_context>
@/Users/jeremyhervo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeremyhervo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ai-on-real-data-context-builders-migration/04-CONTEXT.md
@.planning/phases/04-ai-on-real-data-context-builders-migration/04-RESEARCH.md
</context>

<interfaces>
<!-- Current mock.ts exports that must move to lib/types.ts -->
<!-- Runtime arrays (CLIENTS, PROJECTS, DOCUMENTS, BUDGET_PRODUCTS) stay in mock.ts until plan 04-05 -->

Types to migrate to lib/types.ts:
```typescript
export type ClientCategory = "client" | "prospect" | "archived";
export type ClientStatus = "active" | "draft" | "paused";
export type ProjectType = "brand" | "site" | "campaign" | "social" | "other";
export type ProjectStatus = "active" | "done" | "paused" | "draft";
export type Client = { id: string; name: string; industry: string; category: ClientCategory; status: ClientStatus; contact: { name: string; role: string; email: string; phone?: string }; color: string; since?: string; }
export type Project = { id: string; clientId: string; name: string; type: ProjectType; status: ProjectStatus; description: string; progress: number; totalPhases: number; lastActivity: string; startDate: string; potentialAmount?: number; }
export type Document = { id: string; clientId: string; projectId?: string; name: string; type: "brief" | "platform" | "campaign" | "site" | "other"; updatedAt: string; size: string; content?: string; storagePath?: string; }
export type Conversation = { id: string; clientId: string; projectId?: string; title: string; preview: string; date: string; messageCount: number; }
export type Message = { id: string; role: "user" | "assistant"; content: string; timestamp: string; }
export type PaymentStage = { amount?: number; date?: string; status: "pending" | "sent" | "paid"; }
export type BudgetProduct = { id: string; projectId: string; name: string; totalAmount: number; devis?: PaymentStage; acompte?: PaymentStage; avancement?: PaymentStage; solde?: PaymentStage; }
```

UI config maps to migrate to lib/types.ts (currently in mock.ts lines 532-565):
```typescript
export const PROJECT_STATUS_CONFIG: Record<ProjectStatus, { label: string; color: string }> = { ... }
export const DOC_TYPE_LABEL: Record<Document["type"], string> = { ... }
export const DOC_TYPE_COLOR: Record<Document["type"], string> = { ... }
export const PAYMENT_STAGE_LABEL: Record<"devis" | "acompte" | "avancement" | "solde", string> = { ... }
```

Files currently importing from '@/lib/mock' (run: grep -r "from '@/lib/mock'" src/):
- src/lib/data/documents.ts — type { Document, BudgetProduct, PaymentStage }
- src/hooks/useChat.ts — type { Message }
- src/lib/data/projects.ts — type { Project, ProjectType, ProjectStatus }
- src/lib/context-builders.ts — types + CLIENTS, PROJECTS, DOCUMENTS, BUDGET_PRODUCTS (context-builders keeps mock import until plan 04-02)
- src/lib/data/clients.ts — type { Client, ClientCategory, ClientStatus }
- src/components/AddDocForm.tsx — type { Document }
- src/components/ClientSidebar.tsx — type { Client, ClientCategory }
- src/components/ProductDrawer.tsx — { PAYMENT_STAGE_LABEL, type BudgetProduct, type PaymentStage }
- src/components/ProjectPageShell.tsx — types
- src/components/tabs/ChatTab.tsx — type { Project, Message, Document }
- src/components/ClientPageShell.tsx — types
- src/components/ClientChatDrawer.tsx — { getClient } from mock (keep until plan 04-05 — cannot migrate, it imports runtime function)
- src/components/DocumentViewer.tsx — { DOC_TYPE_LABEL, DOC_TYPE_COLOR, type Document }
- src/components/tabs/ClientChatTab.tsx — type { Client, Message }
- src/components/tabs/ProduitsTab.tsx — { getBudgetProducts, type Project } — getBudgetProducts is mock runtime, keep until 04-05
- src/components/tabs/BudgetsTab.tsx — types + PAYMENT_STAGE_LABEL
- src/components/tabs/DocumentsTab.tsx — { DOC_TYPE_LABEL, DOC_TYPE_COLOR, type Project, type Document }
- src/app/(dashboard)/compta/page.tsx — type { Client }
- src/app/(dashboard)/actions/documents.ts — type { Document }
- src/app/(dashboard)/actions/clients.ts — type { ClientCategory, ClientStatus }
- src/app/(dashboard)/actions/projects.ts — type { ProjectType, ProjectStatus }
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create lib/types.ts with all types and UI config maps</name>
  <files>dashboard/src/lib/types.ts</files>
  <action>
    Create `dashboard/src/lib/types.ts`. This file contains ONLY type definitions and UI config constant maps — NO runtime mock data arrays (those stay in mock.ts until plan 04-05).

    Copy from mock.ts:
    1. All type exports (ClientCategory, ClientStatus, ProjectType, ProjectStatus, Client, Project, Document, Conversation, Message, PaymentStage, BudgetProduct)
    2. UI config maps: PROJECT_STATUS_CONFIG, DOC_TYPE_LABEL, DOC_TYPE_COLOR, PAYMENT_STAGE_LABEL

    Do NOT add `import 'server-only'` — this file is used by both server and client components.

    The file is purely declarative — types + const maps with string values. No imports needed.
  </action>
  <verify>
    <automated>cd "/Users/jeremyhervo/Agence Yam Dropbox/Yambox/_IA/getBrandon/getBrandon/dashboard" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>lib/types.ts exists and exports all required types and config maps. File is importable from both client and server contexts.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate all type imports from @/lib/mock to @/lib/types</name>
  <files>
    dashboard/src/lib/data/clients.ts,
    dashboard/src/lib/data/projects.ts,
    dashboard/src/lib/data/documents.ts,
    dashboard/src/app/(dashboard)/actions/documents.ts,
    dashboard/src/app/(dashboard)/actions/clients.ts,
    dashboard/src/app/(dashboard)/actions/projects.ts,
    dashboard/src/hooks/useChat.ts,
    dashboard/src/components/ClientSidebar.tsx,
    dashboard/src/components/ProductDrawer.tsx,
    dashboard/src/components/ProjectPageShell.tsx,
    dashboard/src/components/ClientPageShell.tsx,
    dashboard/src/components/DocumentViewer.tsx,
    dashboard/src/components/AddDocForm.tsx,
    dashboard/src/components/tabs/ChatTab.tsx,
    dashboard/src/components/tabs/ClientChatTab.tsx,
    dashboard/src/components/tabs/BudgetsTab.tsx,
    dashboard/src/components/tabs/DocumentsTab.tsx,
    dashboard/src/app/(dashboard)/compta/page.tsx
  </files>
  <action>
    For each file listed, update the import that pulls from `@/lib/mock` to pull from `@/lib/types` instead — but ONLY for types and config maps that now live in `lib/types.ts`.

    Rules:
    - `import type { X }` → change to `import type { X } from '@/lib/types'`
    - `import { PAYMENT_STAGE_LABEL, ... }` → change to `import { PAYMENT_STAGE_LABEL, ... } from '@/lib/types'`
    - `import { DOC_TYPE_LABEL, DOC_TYPE_COLOR, ... }` → change to `import { DOC_TYPE_LABEL, DOC_TYPE_COLOR, ... } from '@/lib/types'`

    DO NOT touch these files (they import runtime values from mock that cannot be migrated yet):
    - `src/components/ClientChatDrawer.tsx` — imports `getClient` runtime function
    - `src/components/tabs/ProduitsTab.tsx` — imports `getBudgetProducts` runtime function
    - `src/lib/context-builders.ts` — will be fully replaced in plan 04-02

    Each file may have a single import line from `@/lib/mock`. Replace only the mock-specific symbols that are now in `lib/types.ts`. If a file imports BOTH types AND runtime values (e.g., `ProductDrawer.tsx` imports `PAYMENT_STAGE_LABEL` which is now in types), split the import into two lines: one `from '@/lib/types'` for the config map, and keep the remaining symbols from `@/lib/mock` if any runtime values are still needed.

    After all edits, run `npm run build` to confirm zero TypeScript errors.
  </action>
  <verify>
    <automated>cd "/Users/jeremyhervo/Agence Yam Dropbox/Yambox/_IA/getBrandon/getBrandon/dashboard" && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>npm run build passes. grep -r "from '@/lib/mock'" src/ shows only: context-builders.ts (will be replaced in 04-02), ClientChatDrawer.tsx (runtime import, handled in 04-05), ProduitsTab.tsx (runtime import, handled in 04-05), and any file that truly still needs mock runtime arrays.</done>
</task>

</tasks>

<verification>
After plan completes:
1. `npm run build` passes with zero errors
2. `dashboard/src/lib/types.ts` exists and is non-empty
3. All DAL files (`lib/data/*.ts`) import from `@/lib/types` (not `@/lib/mock`)
4. All Server Actions import from `@/lib/types` (not `@/lib/mock`)
5. UI components import type maps from `@/lib/types`
</verification>

<success_criteria>
- lib/types.ts created with all type exports and UI config maps
- 18+ files updated to import from @/lib/types
- npm run build passes (zero TypeScript errors)
- mock.ts still exists (not deleted yet — that's plan 04-05)
</success_criteria>

<output>
After completion, create `.planning/phases/04-ai-on-real-data-context-builders-migration/04-01-SUMMARY.md`
</output>
