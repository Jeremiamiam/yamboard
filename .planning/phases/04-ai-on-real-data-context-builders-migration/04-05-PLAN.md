---
phase: 04-ai-on-real-data-context-builders-migration
plan: 05
type: execute
wave: 4
depends_on: [04-04]
files_modified:
  - dashboard/src/components/GlobalNav.tsx
  - dashboard/src/components/AgencyChatDrawer.tsx
  - dashboard/src/components/ClientChatDrawer.tsx
  - dashboard/src/components/tabs/ProduitsTab.tsx
  - dashboard/src/lib/mock.ts
  - dashboard/src/lib/doc-content.ts
autonomous: false
requirements: [AI-1, ARCH-5]

must_haves:
  truths:
    - "GlobalNav has a Brandon button that opens AgencyChatDrawer"
    - "AgencyChatDrawer uses useChat({ contextType: 'agency' }) — no clientId or projectId"
    - "AgencyChatDrawer state resets on close (no persistence)"
    - "mock.ts is deleted — npm run build passes with zero remaining @/lib/mock imports"
    - "doc-content.ts is deleted"
    - "ClientChatDrawer no longer imports getClient from mock — uses pathname-based clientId prop"
    - "ProduitsTab no longer imports getBudgetProducts from mock"
  artifacts:
    - path: "dashboard/src/components/AgencyChatDrawer.tsx"
      provides: "Slide-over drawer with agency-scope Brandon chat"
    - path: "dashboard/src/components/GlobalNav.tsx"
      provides: "Brandon button wired to AgencyChatDrawer"
  key_links:
    - from: "dashboard/src/components/GlobalNav.tsx"
      to: "dashboard/src/components/AgencyChatDrawer.tsx"
      via: "useState + AgencyChatDrawer open={agencyChatOpen}"
      pattern: "AgencyChatDrawer"
    - from: "dashboard/src/components/AgencyChatDrawer.tsx"
      to: "dashboard/src/hooks/useChat.ts"
      via: "useChat({ contextType: 'agency' })"
      pattern: "useChat.*agency"
---

<objective>
Final plan of Phase 04: (1) Build the GlobalNav agency chat button and AgencyChatDrawer component, (2) fix the remaining mock.ts runtime consumers (ClientChatDrawer, ProduitsTab), (3) delete mock.ts and doc-content.ts, (4) human checkpoint to verify all 3 chat scopes work end-to-end with real data.

Purpose: Completes the milestone. After this plan, mock.ts is deleted (ARCH-5), the agency-level chat exists globally (AI-1), and the app runs entirely on Supabase data.

Output: AgencyChatDrawer component, GlobalNav wired, mock.ts/doc-content.ts deleted, build clean, all 3 AI scopes verified working.
</objective>

<execution_context>
@/Users/jeremyhervo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeremyhervo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-ai-on-real-data-context-builders-migration/04-CONTEXT.md
@.planning/phases/04-ai-on-real-data-context-builders-migration/04-RESEARCH.md
</context>

<interfaces>
<!-- useChat hook signature (unchanged) -->
From dashboard/src/hooks/useChat.ts:
```typescript
type ChatScope =
  | { contextType: "agency" }
  | { contextType: "client"; clientId: string }
  | { contextType: "project"; clientId: string; projectId: string };

export function useChat(scope: ChatScope): {
  messages: Message[];
  input: string;
  setInput: Dispatch<SetStateAction<string>>;
  isLoading: boolean;
  sendMessage: (text?: string) => Promise<void>;
  handleKeyDown: (e: React.KeyboardEvent<HTMLTextAreaElement>) => void;
}
```

<!-- ClientChatTab (reuse pattern for AgencyChatDrawer) -->
From dashboard/src/components/tabs/ClientChatTab.tsx — study how it renders the chat UI (message list + input + send button). AgencyChatDrawer should match the same visual style using the same Tailwind patterns.

<!-- GlobalNav current right section -->
Currently has: theme toggle, settings button, Y avatar, logout form.
Add Brandon button BEFORE the logout form in the right section.

<!-- ProduitsTab mock usage -->
src/components/tabs/ProduitsTab.tsx imports { getBudgetProducts } from "@/lib/mock"
This is a mock runtime function. After plan 04-02, the DAL has getBudgetProducts() in lib/data/documents.ts.
However, ProduitsTab is a 'use client' component — it cannot directly call the DAL (server-only).
Solution: Check how ProduitsTab is currently wired. If it calls getBudgetProducts() directly from mock (client-side fetch), it must be refactored to receive products as props from the parent Server Component.
Inspect ProduitsTab and its parent to understand current wiring before modifying.

<!-- ClientChatDrawer mock usage -->
Currently: const client = getClient(clientId) — calls mock getClient() client-side
Solution: The client name for the drawer header can be passed as a prop from the parent or resolved from URL + a lightweight fetch. Simplest fix: make ClientChatDrawer accept an optional clientName prop, or remove the client object lookup and just show clientId in header if name not available.
Check context/ClientChatDrawer.tsx to understand how it's opened and what data is available.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create AgencyChatDrawer + wire GlobalNav Brandon button + fix mock runtime consumers</name>
  <files>
    dashboard/src/components/AgencyChatDrawer.tsx,
    dashboard/src/components/GlobalNav.tsx,
    dashboard/src/components/ClientChatDrawer.tsx,
    dashboard/src/components/tabs/ProduitsTab.tsx
  </files>
  <action>
    **Part A — AgencyChatDrawer.tsx (NEW FILE):**
    Create `dashboard/src/components/AgencyChatDrawer.tsx` as a `'use client'` component. Props: `{ open: boolean; onClose: () => void }`.

    Use the same drawer pattern as `ClientChatDrawer.tsx`: fixed overlay + fixed right panel (`fixed inset-y-0 right-0 z-50 w-[70vw] min-w-[320px]`).

    Inside: call `useChat({ contextType: 'agency' })`. Render the same chat UI structure as `ClientChatTab` (or `ChatTab`) — message list, input textarea, send button. Messages state lives in useChat — reset on drawer close automatically (since it's component-local state, unmounting resets it).

    Header: "Brandon — Agence" with a close button.

    Key: `contextType: 'agency'` only — NO clientId or projectId. The agency scope is global.

    **Part B — GlobalNav.tsx:**
    Add `useState(false)` for `agencyChatOpen`. In the right section of the nav, add a "Brandon" button BEFORE the logout form:
    ```typescript
    <button
      onClick={() => setAgencyChatOpen(true)}
      className="px-3 py-1 rounded-md text-xs font-medium bg-emerald-600 text-white hover:bg-emerald-700 transition-colors"
      title="Chat agence — Brandon"
    >
      Brandon
    </button>
    ```
    At the end of the return, render: `<AgencyChatDrawer open={agencyChatOpen} onClose={() => setAgencyChatOpen(false)} />`

    **Part C — Fix ClientChatDrawer.tsx (remove mock getClient import):**
    Currently: `const client = getClient(clientId)` — this calls a mock runtime function.
    Fix: Read `ClientChatDrawer.tsx` and its context (`context/ClientChatDrawer.tsx`) to understand what `client` is used for. It's only used for `client.name` in the header.
    Solution: Accept `clientName?: string` as a prop on `ClientChatDrawer`, or read the client name from the existing page props if available. Simplest: remove the `getClient` call, and instead show just "Chat client" as the header title (or extract clientId from pathname as fallback). The chat itself only needs `clientId` via `useChat`, which it gets from the pathname.
    Remove the `import { getClient } from "@/lib/mock"` line entirely.

    **Part D — Fix ProduitsTab.tsx (remove mock getBudgetProducts import):**
    Inspect the component. If `getBudgetProducts` from mock is called inside the component directly (client-side), it needs to be refactored. Check its parent (ProjectPageShell or similar) to see if products are already passed as props.
    If the parent already fetches products from the Supabase DAL and passes them as props: ProduitsTab may already receive them as props but also have a fallback mock call. Remove the mock import and the fallback mock call — use props only.
    If ProduitsTab calls getBudgetProducts from mock as the sole data source: the parent (ProjectPageShell) must be updated to pass products from the DAL. Check how ProjectPageShell is currently wired for other data (it should already fetch from Supabase).

    Goal: zero `from '@/lib/mock'` imports in ProduitsTab.tsx.
  </action>
  <verify>
    <automated>cd "/Users/jeremyhervo/Agence Yam Dropbox/Yambox/_IA/getBrandon/getBrandon/dashboard" && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>npm run build passes. AgencyChatDrawer.tsx exists. GlobalNav has Brandon button. ClientChatDrawer.tsx has no mock import. ProduitsTab.tsx has no mock import.</done>
</task>

<task type="auto">
  <name>Task 2: Delete mock.ts and doc-content.ts — verify zero remaining imports</name>
  <files>
    dashboard/src/lib/mock.ts,
    dashboard/src/lib/doc-content.ts
  </files>
  <action>
    Before deleting, run a final check:
    ```bash
    grep -r "from '@/lib/mock'" /path/to/dashboard/src/
    grep -r "from '@/lib/doc-content'" /path/to/dashboard/src/
    ```

    If any remaining imports exist (other than context-builders.ts which was replaced in 04-04), fix them before deleting.

    Then delete both files:
    - `dashboard/src/lib/mock.ts`
    - `dashboard/src/lib/doc-content.ts`

    Run `npm run build` immediately after deletion. If build fails due to remaining imports, fix the offending file and re-run. Do not re-create mock.ts — fix the consumer.

    Common remaining consumers to check:
    - `src/lib/context-builders.ts` — should have no mock imports after 04-04
    - `src/components/ClientChatDrawer.tsx` — fixed in Task 1 of this plan
    - `src/components/tabs/ProduitsTab.tsx` — fixed in Task 1 of this plan

    After successful deletion + clean build, confirm TypeScript compile:
    ```bash
    npx tsc --noEmit
    ```
  </action>
  <verify>
    <automated>cd "/Users/jeremyhervo/Agence Yam Dropbox/Yambox/_IA/getBrandon/getBrandon/dashboard" && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>mock.ts deleted. doc-content.ts deleted. npm run build passes. tsc --noEmit clean. grep finds zero remaining imports from @/lib/mock or @/lib/doc-content.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 04 implementation:
    - 3 async context builders on real Supabase data (agency, client, project scopes)
    - Per-scope model selection (Haiku for agency, Opus for client/project)
    - Token budget enforcement with console.warn logging
    - XML structural tags in all context strings (AI-6)
    - PDF/txt content extraction at upload time
    - AgencyChatDrawer in GlobalNav
    - mock.ts and doc-content.ts deleted
  </what-built>
  <how-to-verify>
    1. Run: `cd dashboard && npm run dev`
    2. Login to the app
    3. **Test agency scope:** Click "Brandon" in the top nav → drawer opens → ask "Quels sont nos clients actifs ?" → verify response mentions real client names from Supabase (not mock names)
    4. **Test client scope:** Navigate to a client → open chat → ask "Quels sont les projets en cours ?" → verify real project names in response
    5. **Test project scope:** Navigate to a project → open chat → ask "Résume ce projet" → verify real project data in response
    6. **Test token logging:** In terminal, check console output → verify "[chat] contextType=agency input_tokens=..." log appears after each message
    7. **Test agency chat reset:** Close and reopen agency chat drawer → conversation is empty (no persistence)
    8. **Test PDF upload (optional):** Upload a PDF to a project → check Supabase Studio documents table → verify content column is populated with extracted text
  </how-to-verify>
  <resume-signal>Type "approved" if all 3 scopes respond with real Supabase data, or describe specific issues to fix.</resume-signal>
</task>

</tasks>

<verification>
Phase 04 complete when:
1. `npm run build` passes
2. `grep -r "from '@/lib/mock'" src/` returns zero results
3. `grep -r "from '@/lib/doc-content'" src/` returns zero results
4. All 3 chat scopes respond with real data (human verified)
5. input_tokens logged in console after each chat response
6. AgencyChatDrawer accessible from GlobalNav
</verification>

<success_criteria>
- GlobalNav "Brandon" button opens agency chat drawer
- Agency chat uses real Supabase client/project data
- Client chat and project chat use real Supabase data
- mock.ts deleted, doc-content.ts deleted
- Zero TypeScript errors
- input_tokens logged per request
- Human verified: all 3 scopes respond correctly
</success_criteria>

<output>
After completion, create `.planning/phases/04-ai-on-real-data-context-builders-migration/04-05-SUMMARY.md`
</output>
